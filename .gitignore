.env
.env.*
!.env.example
node_modules/
dist/
*.log

# Worker images decoded to local disk by prisma/master-worker.seed.ts (WORKER_IMAGE_STORAGE_DIR) and
# Admin profile images uploaded to local disk via POST /api/auth/me/upload-image (ADMIN_IMAGE_STORAGE_DIR)
/storage/worker-images/
/storage/admin-images/

# SSH deploy keys (see README > CI/CD auto-deploy) — never commit these
deploy_key*
*.pem
id_rsa*
id_ed25519*
