{
  "info": {
    "_postman_id": "0f55f6b6-4a4f-4f5f-8b75-9f5d37a07f38",
    "name": "Labor Project Backend",
    "description": "Postman collection for Labor Project Backend. baseUrl defaults to http://localhost:8080 for local development — change the baseUrl/wsBaseUrl variables to the shared droplet's domain (see README > Deploying to DigitalOcean) to test against the shared dev/test deployment instead.\nEvery request shows its contract explicitly in that request: method, URL, required headers, query params, and body.\nProtected requests use Header: Authorization: Bearer {{accessToken}}.\nRun Auth > POST /api/auth/login first to get AccessToken and RefreshToken.\nEvery request also accepts optional X-Request-Id and X-Client-Version headers for log correlation/debugging (added automatically below). X-Client-Type is required only on the /api/auth endpoints that run before an access token exists (login, login/confirm-force, refresh) — see each request's Headers line.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "auth": {
    "type": "noauth"
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": []
      }
    },
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": []
      }
    }
  ],
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8080",
      "type": "string",
      "description": "Backend base URL for testing. Set to https://<your-domain> to test against the shared droplet dev/test deployment instead — see README > Deploying to DigitalOcean."
    },
    {
      "key": "wsBaseUrl",
      "value": "ws://localhost:8080",
      "type": "string",
      "description": "Backend WebSocket base URL for Worker Mobile realtime connection. Set to wss://<your-domain> for the shared droplet deployment."
    },
    {
      "key": "accessToken",
      "value": "",
      "type": "string",
      "description": "Bearer access token returned by POST /api/auth/login. Used in Authorization header."
    },
    {
      "key": "refreshToken",
      "value": "",
      "type": "string",
      "description": "Refresh token returned by POST /api/auth/login. Used by POST /api/auth/refresh body."
    },
    {
      "key": "loginChallengeToken",
      "value": "",
      "type": "string",
      "description": "Token returned when login gets ACTIVE_SESSION_EXISTS. Used by confirm-force login."
    },
    {
      "key": "deviceId",
      "value": "postman",
      "type": "string",
      "description": "Client device id sent in login body."
    },
    {
      "key": "deviceName",
      "value": "Postman",
      "type": "string",
      "description": "Client device name sent in login body."
    },
    {
      "key": "fcmToken",
      "value": "postman-fcm-token",
      "type": "string",
      "description": "Worker Firebase Cloud Messaging token for push notification tests."
    },
    {
      "key": "pushPlatform",
      "value": "android",
      "type": "string",
      "description": "Worker push token platform: android, ios, web, or unknown."
    },
    {
      "key": "gateClientId",
      "value": "gate-main",
      "type": "string",
      "description": "Gate ClientId used by POST /api/gate/tickets Basic Auth."
    },
    {
      "key": "gateClientSecret",
      "value": "",
      "type": "string",
      "description": "Gate ClientSecret shown once by Admin Settings create/rotate Gate client APIs."
    },
    {
      "key": "workerCode",
      "value": "CB000007",
      "type": "string",
      "description": "WorkerCode used by Admin Workers /:workerCode routes. Real MasterWorker seeded locally by prisma/master-worker.seed.ts."
    },
    {
      "key": "workerUsername",
      "value": "CB000007",
      "type": "string",
      "description": "Worker login Username for POST /api/auth/login - Worker requests. Real MasterWorker seeded locally (prisma/master-worker.seed.ts)."
    },
    {
      "key": "workerPassword",
      "value": "0655924660",
      "type": "string",
      "description": "Worker login Password for {{workerUsername}} — the worker's telephone digits with no dashes (seed sets password = telephone, dashes stripped)."
    },
    {
      "key": "workerUsername2",
      "value": "CG000004",
      "type": "string",
      "description": "A second real seeded WorkerCode, for scenarios needing more than one worker (e.g. team check-in/accept flows)."
    },
    {
      "key": "workerPassword2",
      "value": "0951094560",
      "type": "string",
      "description": "Worker login Password for {{workerUsername2}} — telephone digits with no dashes."
    },
    {
      "key": "adminUserId",
      "value": "",
      "type": "string",
      "description": "Lower-rank admin account id used by /api/admin/users/{{adminUserId}}/permissions. Set by POST /api/admin/admins."
    },
    {
      "key": "ticketNumber",
      "value": "20260723000001",
      "type": "string",
      "description": "TicketNumber (vehicle-level identity) returned as the top-level TicketNumber field by POST /api/gate/tickets. Used by all /api/admin/vehicle-jobs/{ticketNumber}/... routes. Do not confuse with ticketNo, which is the Business Ticket identity (Ticket.TicketNo)."
    },
    {
      "key": "ticketNo",
      "value": "20260804000002",
      "type": "string",
      "description": "ticketNo returned by POST /api/gate/tickets. With the Gate payload this equals TicketNo."
    },
    {
      "key": "marketCode",
      "value": "1123",
      "type": "string",
      "description": "marketCode returned by Admin history/operations. With the Gate payload this equals MarketCode."
    },
    {
      "key": "boothCode",
      "value": "OM3/40",
      "type": "string",
      "description": "boothCode used by admin stall APIs and worker ticket completion. Gate Flow saves the first Booths[] item here for follow-up requests."
    },
    {
      "key": "stallCode",
      "value": "OM3/40",
      "type": "string",
      "description": "stallCode path variable for admin /stalls routes. It uses the same value as BoothCode."
    },
    {
      "key": "productCode",
      "value": "02020300",
      "type": "string",
      "description": "productCode used by worker product quantity confirmation. Gate Flow saves the first product from the first Booths[] item here for follow-up requests."
    },
    {
      "key": "packageCode",
      "value": "29"
    },
    {
      "key": "vendorConfirmToken",
      "value": "",
      "type": "string",
      "description": "Manual test only. Signed token for vendor confirm if copied from a LINE button payload or delivery log. Real vendors normally tap the LINE button directly."
    },
    {
      "key": "vendorRejectToken",
      "value": "",
      "type": "string",
      "description": "Manual test only. Signed token for vendor reject if copied from a LINE button payload or delivery log. Real vendors normally tap the LINE button directly."
    },
    {
      "key": "vendorRatingToken",
      "value": "",
      "type": "string",
      "description": "Manual test only. Signed token for vendor rating if copied from the LINE rating payload or delivery log. Real vendors normally tap the LINE rating button directly."
    },
    {
      "key": "lineUserId",
      "value": "Ubd9b17811181e01f8fca0372d2b37088",
      "type": "string",
      "description": "LINE User ID mapped in seed for manual LINE webhook tests. Real LINE webhooks send this as source.userId automatically."
    },
    {
      "key": "lineDevSubmissionId",
      "value": "",
      "type": "string",
      "description": "SubmissionId to confirm/reject from GET /api/line/dev/submissions when using the LINE dev tester fallback endpoints."
    },
    {
      "key": "driverQrToken",
      "value": "",
      "type": "string",
      "description": "Driver QR token returned by POST /api/gate/tickets."
    },
    {
      "key": "driverSessionToken",
      "value": "",
      "type": "string",
      "description": "Driver guest session token returned by POST /api/driver/qr-sessions."
    },
    {
      "key": "mobileAppVersionId",
      "value": ""
    },
    {
      "key": "clientVersion",
      "value": "1.0.0",
      "type": "string",
      "description": "Example app version sent via the X-Client-Version header (log/debugging only, not required)."
    },
    {
      "key": "clientType",
      "value": "worker_app",
      "type": "string",
      "description": "X-Client-Type value for endpoints where the role isn't known yet (e.g. POST /api/auth/refresh). Switch to admin_webapp when testing an Admin refresh."
    }
  ],
  "item": [
    {
      "name": "Auth",
      "item": [
        {
          "name": "POST /api/auth/login - Admin",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let body = {};",
                  "try { body = pm.response.json(); } catch (error) {}",
                  "if (body.AccessToken) pm.collectionVariables.set('accessToken', body.AccessToken);",
                  "if (body.RefreshToken) pm.collectionVariables.set('refreshToken', body.RefreshToken);",
                  "if (body.LoginChallengeToken) pm.collectionVariables.set('loginChallengeToken', body.LoginChallengeToken);",
                  "if (body.Details && body.Details.LoginChallengeToken) pm.collectionVariables.set('loginChallengeToken', body.Details.LoginChallengeToken);"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text",
                "description": "Required when sending JSON body."
              },
              {
                "key": "X-Client-Type",
                "value": "admin_webapp",
                "type": "text",
                "description": "Required here: no access token exists yet for the server to read the role from, and this path is shared by every app. Values: worker_app, admin_webapp, driver_webapp, gate, line_oa."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Username\": \"admin\",\n  \"Password\": \"Admin@123456\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "login"
              ]
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/auth/login\nAuth: No auth required\nHeaders: Content-Type: application/json, X-Client-Type (required, admin_webapp), X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: Admin username/password only.\nResponse: login returns tokens only: AccessToken, RefreshToken, TokenType, ExpiresIn.\nNotes: Saves AccessToken and RefreshToken into collection variables when login succeeds. Call GET /api/auth/me after login to fetch admin profile data, permissions, and latest activity."
          },
          "response": []
        },
        {
          "name": "POST /api/auth/login - Worker 1",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let body = {};",
                  "try { body = pm.response.json(); } catch (error) {}",
                  "if (body.AccessToken) pm.collectionVariables.set('accessToken', body.AccessToken);",
                  "if (body.RefreshToken) pm.collectionVariables.set('refreshToken', body.RefreshToken);",
                  "if (body.LoginChallengeToken) pm.collectionVariables.set('loginChallengeToken', body.LoginChallengeToken);",
                  "if (body.Details && body.Details.LoginChallengeToken) pm.collectionVariables.set('loginChallengeToken', body.Details.LoginChallengeToken);"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text",
                "description": "Required when sending JSON body."
              },
              {
                "key": "X-Client-Type",
                "value": "worker_app",
                "type": "text",
                "description": "Required here: no access token exists yet for the server to read the role from, and this path is shared by every app. Values: worker_app, admin_webapp, driver_webapp, gate, line_oa."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Username\": \"CG000004\",\n  \"Password\": \"0951094560\",\n  \"DeviceId\": \"{{deviceId}}\",\n  \"DeviceName\": \"{{deviceName}}\",\n  \"FcmToken\": \"{{fcmToken}}\",\n  \"Platform\": \"{{pushPlatform}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "login"
              ]
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/auth/login\nAuth: No auth required\nHeaders: Content-Type: application/json, X-Client-Type (required, worker_app), X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: Worker CG000004 (กะเช้า 06:00-18:00 จาก seed) — Password คือเบอร์โทรจาก seed. DeviceId, DeviceName, optional FcmToken, and optional Platform.\nResponse: login returns tokens only: AccessToken, RefreshToken, TokenType, ExpiresIn. FcmToken is not returned.\nNotes: Saves AccessToken and RefreshToken into collection variables when login succeeds. If another worker session exists on a different device, saves LoginChallengeToken for POST /api/auth/login/confirm-force. When FcmToken is sent by a worker, backend stores it by WorkerCode + DeviceId + Platform for push notifications. If Mobile does not have an FCM token yet, omit FcmToken here and call POST /api/auth/push-token after Firebase SDK returns the token. Call GET /api/auth/me after login to fetch worker profile data. After successful worker login, open WS /ws/workers, then call POST /api/workers/me/online."
          },
          "response": []
        },
        {
          "name": "POST /api/auth/login - Worker 2",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let body = {};",
                  "try { body = pm.response.json(); } catch (error) {}",
                  "if (body.AccessToken) pm.collectionVariables.set('accessToken', body.AccessToken);",
                  "if (body.RefreshToken) pm.collectionVariables.set('refreshToken', body.RefreshToken);",
                  "if (body.LoginChallengeToken) pm.collectionVariables.set('loginChallengeToken', body.LoginChallengeToken);",
                  "if (body.Details && body.Details.LoginChallengeToken) pm.collectionVariables.set('loginChallengeToken', body.Details.LoginChallengeToken);"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text",
                "description": "Required when sending JSON body."
              },
              {
                "key": "X-Client-Type",
                "value": "worker_app",
                "type": "text",
                "description": "Required here: no access token exists yet for the server to read the role from, and this path is shared by every app. Values: worker_app, admin_webapp, driver_webapp, gate, line_oa."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Username\": \"CG000005\",\n  \"Password\": \"0950039059\",\n  \"DeviceId\": \"{{deviceId}}\",\n  \"DeviceName\": \"{{deviceName}}\",\n  \"FcmToken\": \"{{fcmToken}}\",\n  \"Platform\": \"{{pushPlatform}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "login"
              ]
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/auth/login\nAuth: No auth required\nHeaders: Content-Type: application/json, X-Client-Type (required, worker_app), X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: Worker CG000005 (กะเช้า 06:00-18:00 จาก seed) — Password คือเบอร์โทรจาก seed. DeviceId, DeviceName, optional FcmToken, and optional Platform.\nResponse: login returns tokens only: AccessToken, RefreshToken, TokenType, ExpiresIn. FcmToken is not returned.\nNotes: Saves AccessToken and RefreshToken into collection variables when login succeeds. If another worker session exists on a different device, saves LoginChallengeToken for POST /api/auth/login/confirm-force. When FcmToken is sent by a worker, backend stores it by WorkerCode + DeviceId + Platform for push notifications. If Mobile does not have an FCM token yet, omit FcmToken here and call POST /api/auth/push-token after Firebase SDK returns the token. Call GET /api/auth/me after login to fetch worker profile data. After successful worker login, open WS /ws/workers, then call POST /api/workers/me/online."
          },
          "response": []
        },
        {
          "name": "POST /api/auth/login - Worker 3",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let body = {};",
                  "try { body = pm.response.json(); } catch (error) {}",
                  "if (body.AccessToken) pm.collectionVariables.set('accessToken', body.AccessToken);",
                  "if (body.RefreshToken) pm.collectionVariables.set('refreshToken', body.RefreshToken);",
                  "if (body.LoginChallengeToken) pm.collectionVariables.set('loginChallengeToken', body.LoginChallengeToken);",
                  "if (body.Details && body.Details.LoginChallengeToken) pm.collectionVariables.set('loginChallengeToken', body.Details.LoginChallengeToken);"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text",
                "description": "Required when sending JSON body."
              },
              {
                "key": "X-Client-Type",
                "value": "worker_app",
                "type": "text",
                "description": "Required here: no access token exists yet for the server to read the role from, and this path is shared by every app. Values: worker_app, admin_webapp, driver_webapp, gate, line_oa."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Username\": \"CB000007\",\n  \"Password\": \"0655924660\",\n  \"DeviceId\": \"{{deviceId}}\",\n  \"DeviceName\": \"{{deviceName}}\",\n  \"FcmToken\": \"{{fcmToken}}\",\n  \"Platform\": \"{{pushPlatform}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "login"
              ]
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/auth/login\nAuth: No auth required\nHeaders: Content-Type: application/json, X-Client-Type (required, worker_app), X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: Worker CB000007 (กะเย็น 18:00-06:00 จาก seed) — Password คือเบอร์โทรจาก seed. DeviceId, DeviceName, optional FcmToken, and optional Platform.\nResponse: login returns tokens only: AccessToken, RefreshToken, TokenType, ExpiresIn. FcmToken is not returned.\nNotes: Saves AccessToken and RefreshToken into collection variables when login succeeds. If another worker session exists on a different device, saves LoginChallengeToken for POST /api/auth/login/confirm-force. When FcmToken is sent by a worker, backend stores it by WorkerCode + DeviceId + Platform for push notifications. If Mobile does not have an FCM token yet, omit FcmToken here and call POST /api/auth/push-token after Firebase SDK returns the token. Call GET /api/auth/me after login to fetch worker profile data. After successful worker login, open WS /ws/workers, then call POST /api/workers/me/online."
          },
          "response": []
        },
        {
          "name": "POST /api/auth/login - Worker 4",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let body = {};",
                  "try { body = pm.response.json(); } catch (error) {}",
                  "if (body.AccessToken) pm.collectionVariables.set('accessToken', body.AccessToken);",
                  "if (body.RefreshToken) pm.collectionVariables.set('refreshToken', body.RefreshToken);",
                  "if (body.LoginChallengeToken) pm.collectionVariables.set('loginChallengeToken', body.LoginChallengeToken);",
                  "if (body.Details && body.Details.LoginChallengeToken) pm.collectionVariables.set('loginChallengeToken', body.Details.LoginChallengeToken);"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text",
                "description": "Required when sending JSON body."
              },
              {
                "key": "X-Client-Type",
                "value": "worker_app",
                "type": "text",
                "description": "Required here: no access token exists yet for the server to read the role from, and this path is shared by every app. Values: worker_app, admin_webapp, driver_webapp, gate, line_oa."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Username\": \"CB000144\",\n  \"Password\": \"0633721990\",\n  \"DeviceId\": \"{{deviceId}}\",\n  \"DeviceName\": \"{{deviceName}}\",\n  \"FcmToken\": \"{{fcmToken}}\",\n  \"Platform\": \"{{pushPlatform}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "login"
              ]
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/auth/login\nAuth: No auth required\nHeaders: Content-Type: application/json, X-Client-Type (required, worker_app), X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: Worker CB000144 (กะเย็น 18:00-06:00 จาก seed) — Password คือเบอร์โทรจาก seed. DeviceId, DeviceName, optional FcmToken, and optional Platform.\nResponse: login returns tokens only: AccessToken, RefreshToken, TokenType, ExpiresIn. FcmToken is not returned.\nNotes: Saves AccessToken and RefreshToken into collection variables when login succeeds. If another worker session exists on a different device, saves LoginChallengeToken for POST /api/auth/login/confirm-force. When FcmToken is sent by a worker, backend stores it by WorkerCode + DeviceId + Platform for push notifications. If Mobile does not have an FCM token yet, omit FcmToken here and call POST /api/auth/push-token after Firebase SDK returns the token. Call GET /api/auth/me after login to fetch worker profile data. After successful worker login, open WS /ws/workers, then call POST /api/workers/me/online."
          },
          "response": []
        },
        {
          "name": "POST /api/auth/login - Worker without FCM token",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let body = {};",
                  "try { body = pm.response.json(); } catch (error) {}",
                  "if (body.AccessToken) pm.collectionVariables.set('accessToken', body.AccessToken);",
                  "if (body.RefreshToken) pm.collectionVariables.set('refreshToken', body.RefreshToken);",
                  "if (body.LoginChallengeToken) pm.collectionVariables.set('loginChallengeToken', body.LoginChallengeToken);",
                  "if (body.Details && body.Details.LoginChallengeToken) pm.collectionVariables.set('loginChallengeToken', body.Details.LoginChallengeToken);"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text",
                "description": "Required when sending JSON body."
              },
              {
                "key": "X-Client-Type",
                "value": "worker_app",
                "type": "text",
                "description": "Required here: no access token exists yet for the server to read the role from, and this path is shared by every app. Values: worker_app, admin_webapp, driver_webapp, gate, line_oa."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Username\": \"{{workerUsername}}\",\n  \"Password\": \"{{workerPassword}}\",\n  \"DeviceId\": \"{{deviceId}}\",\n  \"DeviceName\": \"{{deviceName}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "login"
              ]
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/auth/login\nAuth: No auth required\nHeaders: Content-Type: application/json, X-Client-Type (required, worker_app), X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: Worker username plus password=phone, DeviceId, and DeviceName. FcmToken is intentionally omitted in this example.\nResponse: login returns tokens only: AccessToken, RefreshToken, TokenType, ExpiresIn.\nNotes: Use this when Mobile has not received an FCM token from Firebase SDK yet. After login succeeds and Firebase SDK returns the token, call POST /api/auth/push-token with FcmToken and Platform."
          },
          "response": []
        },
        {
          "name": "POST /api/auth/login/confirm-force",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let body = {};",
                  "try { body = pm.response.json(); } catch (error) {}",
                  "if (body.AccessToken) pm.collectionVariables.set('accessToken', body.AccessToken);",
                  "if (body.RefreshToken) pm.collectionVariables.set('refreshToken', body.RefreshToken);"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text",
                "description": "Required when sending JSON body."
              },
              {
                "key": "X-Client-Type",
                "value": "worker_app",
                "type": "text",
                "description": "Required here: no access token exists yet for the server to read the role from, and this path is shared by every app. Values: worker_app, admin_webapp, driver_webapp, gate, line_oa."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"LoginChallengeToken\": \"{{loginChallengeToken}}\",\n  \"DeviceId\": \"{{deviceId}}\",\n  \"DeviceName\": \"{{deviceName}}\",\n  \"FcmToken\": \"{{fcmToken}}\",\n  \"Platform\": \"{{pushPlatform}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/login/confirm-force",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "login",
                "confirm-force"
              ]
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/auth/login/confirm-force\nAuth: No auth required\nHeaders: Content-Type: application/json, X-Client-Type (required, worker_app), X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: JSON: LoginChallengeToken, DeviceId, DeviceName, optional FcmToken, optional Platform\nNotes: Use only after login returns ACTIVE_SESSION_EXISTS with a LoginChallengeToken. If FcmToken is sent, backend revokes the old session push token and stores the new token for this WorkerCode. If Mobile does not have an FCM token yet, omit it here and call POST /api/auth/push-token after Firebase SDK returns the token. The push notification sent to the old device (telling it it was signed out) is best-effort only: if it fails for any reason (Firebase/network issues, invalid credential, expired token, etc.), this request still returns 200 and still revokes the old session and creates the new one."
          },
          "response": []
        },
        {
          "name": "POST /api/auth/refresh",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let body = {};",
                  "try { body = pm.response.json(); } catch (error) {}",
                  "if (body.AccessToken) pm.collectionVariables.set('accessToken', body.AccessToken);",
                  "if (body.RefreshToken) pm.collectionVariables.set('refreshToken', body.RefreshToken);"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text",
                "description": "Required when sending JSON body."
              },
              {
                "key": "X-Client-Type",
                "value": "{{clientType}}",
                "type": "text",
                "description": "Required here: no access token exists yet for the server to read the role from, and this path is shared by every app. Values: worker_app, admin_webapp, driver_webapp, gate, line_oa."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"RefreshToken\": \"{{refreshToken}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/refresh",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "refresh"
              ]
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/auth/refresh\nAuth: No auth required\nHeaders: Content-Type: application/json, X-Client-Type (required, {{clientType}}), X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: JSON: RefreshToken\nNotes: Returns a new AccessToken and RefreshToken."
          },
          "response": []
        },
        {
          "name": "POST /api/auth/logout",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text",
                "description": "Required for protected routes. Format: Bearer <AccessToken>."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/auth/logout",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "logout"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/auth/logout\nAuth: Required via Authorization header\nHeaders: Authorization: Bearer {{accessToken}}, X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: None\nNotes: Backend reads session_id from access token and revokes that session."
          },
          "response": []
        },
        {
          "name": "POST /api/auth/push-token",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text",
                "description": "Required for protected routes. Format: Bearer <AccessToken>."
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"FcmToken\": \"{{fcmToken}}\",\n  \"Platform\": \"{{pushPlatform}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/push-token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "push-token"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/auth/push-token\nAuth: Worker access token\nBody: FcmToken required, Platform optional. DeviceId is optional and defaults to the current session DeviceId.\nResponse: StatusCode, Code, Message, WorkerCode, DeviceId, Platform. FcmToken is not returned.\nNotes: Use after login when FcmToken was not available during Auth login, or whenever Firebase SDK returns a refreshed token. This endpoint does not request a token from Firebase; Mobile must obtain the FCM token from Firebase SDK first. Backend stores token by WorkerCode + DeviceId + Platform and uses it for worker push notifications."
          },
          "response": []
        },
        {
          "name": "GET /api/auth/me",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text",
                "description": "Required for protected routes. Format: Bearer <AccessToken>."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/auth/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "me"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/auth/me\nAuth: Required via Authorization header\nHeaders: Authorization: Bearer {{accessToken}}, X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: None\nNotes: Returns current account from access token. Response includes role, FullName, and EmployeeCode for both admin and worker. Admin keeps AdminCode/PermissionLevel/permissions. Worker keeps WorkerCode/profile fields. Worker also gets ShiftActive (boolean): true when the worker is currently inside their assigned shift — the same condition POST /api/workers/me/online checks before throwing 403 OutsideWorkShift — so the app can enable/disable the Online button without recomputing the shift-time comparison itself."
          },
          "response": []
        },
        {
          "name": "PATCH /api/auth/me/password",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text",
                "description": "Required for protected routes. Format: Bearer <AccessToken>."
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"CurrentPassword\": \"Admin@123456\",\n  \"NewPassword\": \"Admin@654321\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/auth/me/password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "auth",
                "me",
                "password"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: PATCH\nURL: {{baseUrl}}/api/auth/me/password\nAuth: Required via Authorization header\nHeaders: Authorization: Bearer {{accessToken}}, Content-Type: application/json, X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: CurrentPassword and NewPassword\nNotes: Works for both Admin and Worker. Backend verifies CurrentPassword, hashes NewPassword, keeps the current session active, and revokes other active sessions for the same account."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Admin Workers",
      "item": [
        {
          "name": "GET /api/admin/users",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text",
                "description": "Required for protected routes. Format: Bearer <AccessToken>."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/users?page=1&limit=20&search=&status=",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "users"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1"
                },
                {
                  "key": "limit",
                  "value": "20"
                },
                {
                  "key": "search",
                  "value": ""
                },
                {
                  "key": "status",
                  "value": "",
                  "description": "Optional. Matches vehicle/market/stall/confirmation/assignment status. Supported examples: WAIT, WORKING, DELIVERED, REJECT, COMPLETED, CANCELLED, PENDING, ACCEPTED, SCANNED, TIMEOUT."
                }
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/users?page=1&limit=20&search=&status=\nAuth: Required via Authorization header\nHeaders: Authorization: Bearer {{accessToken}}, X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: page, limit, search, status\nBody: None\nNotes: Admin only. Lists worker accounts newest first. Response item is trimmed for worker table UI only: WorkerCode, ShirtNumber, FullName, Phone, WorkStartDate, WorkSchedule, Status, UpdatedAt. Shift availability uses only TimeIn and TimeOut; WorkSchedule does not return WorkDate."
          },
          "response": []
        },
        {
          "name": "GET /api/admin/users/:workerCode",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text",
                "description": "Required for protected routes. Format: Bearer <AccessToken>."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/users/{{workerCode}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "users",
                "{{workerCode}}"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/users/{{workerCode}}\nAuth: Required via Authorization header\nHeaders: Authorization: Bearer {{accessToken}}, X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: None\nNotes: Admin only. {{workerCode}} must be WorkerCode, for example MN000012. Response is trimmed for worker edit UI: ImageUrl, WorkerCode, FullName, Status, and Details. Details includes Phone, Position, Nationality, ShirtNumber, ShirtType, WorkStartDate, TimeWork, TimeIn, TimeOut, and ShiftName."
          },
          "response": []
        },
        {
          "name": "POST /api/admin/users",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text",
                "description": "Required for protected routes. Format: Bearer <AccessToken>."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "formdata",
              "formdata": [
                {
                  "key": "ImageUrl",
                  "value": "",
                  "type": "text",
                  "description": "Optional worker profile image URL. Leave blank or omit for null — the backend never requires this field."
                },
                {
                  "key": "FullName",
                  "value": "Worker One",
                  "type": "text"
                },
                {
                  "key": "Phone",
                  "value": "0812345678",
                  "type": "text"
                },
                {
                  "key": "Nationality",
                  "value": "Myanmar",
                  "type": "text"
                },
                {
                  "key": "ShirtType",
                  "value": "Navy",
                  "type": "text"
                },
                {
                  "key": "ShirtNumber",
                  "value": "12",
                  "type": "text"
                },
                {
                  "key": "WorkStartDate",
                  "value": "2026-07-03",
                  "type": "text",
                  "description": "Optional. First working date. If omitted, backend uses the current Bangkok date."
                },
                {
                  "key": "TimeWork",
                  "value": "Morning",
                  "type": "text",
                  "description": "Required. Morning = shift preset 08:00-18:00, Evening = shift preset 18:00-08:00."
                },
                {
                  "key": "Status",
                  "value": "active",
                  "type": "text"
                }
              ]
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/users",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "users"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/admin/users\nAuth: Required via Authorization header\nHeaders: Authorization: Bearer {{accessToken}}. Do not set Content-Type manually for multipart/form-data; Postman sets the boundary., X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: multipart/form-data. ImageUrl (text, optional) — leave blank or omit entirely for null; the frontend passes a URL string here (e.g. a pre-uploaded CDN link), not a file. Text fields: FullName, Phone, Nationality, ShirtType, ShirtNumber, TimeWork, optional WorkStartDate, Status.\nResponse: message only, Worker created successfully.\nNotes: Admin only. username defaults to WorkerCode generated from nationality + ShirtType + ShirtNumber, such as MN000012. nationality accepts Myanmar or Cambodia. ShirtType accepts Navy, Blue, or Green. ShirtNumber is unique and cannot be reused. Worker password is always the hashed value of phone, and phone is saved to accounts.phone. TimeWork Morning uses preset 08:00-18:00 and TimeWork Evening uses preset 18:00-08:00. WorkStartDate is optional; if omitted, backend uses the current Bangkok date. ImageUrl is saved as-is to accounts.image_url — no upload/processing happens server-side for this field. A real binary file upload is still accepted too, via a separate multipart file field named Image (not shown in this example) which the backend saves under /uploads/workers and uses instead of ImageUrl if both are sent."
          },
          "response": []
        },
        {
          "name": "PATCH /api/admin/users/:workerCode",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text",
                "description": "Required for protected routes. Format: Bearer <AccessToken>."
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text",
                "description": "Required when sending JSON body."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"ImageUrl\": \"https://example.com/uploads/workers/mn000012.jpg\",\n  \"FullName\": \"Worker One Updated\",\n  \"Phone\": \"0811111111\",\n  \"Nationality\": \"Myanmar\",\n  \"Status\": \"active\",\n  \"Position\": \"Worker\",\n  \"ShirtNumber\": \"12\",\n  \"ShirtType\": \"Navy\",\n  \"WorkStartDate\": \"2026-07-03\",\n  \"TimeIn\": \"08:00\",\n  \"TimeOut\": \"19:00\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/users/{{workerCode}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "users",
                "{{workerCode}}"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: PATCH\nURL: {{baseUrl}}/api/admin/users/{{workerCode}}\nAuth: Required via Authorization header\nHeaders: Authorization: Bearer {{accessToken}}, Content-Type: application/json, X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: JSON fields from worker edit UI: ImageUrl/img, WorkerCode, FullName, Phone, Nationality, Status, Position, ShirtNumber, ShirtType, optional WorkStartDate, TimeIn, TimeOut. nationality accepts Myanmar or Cambodia. ShirtType accepts Navy, Blue, or Green. ShirtNumber is unique and cannot be reused. TimeWork cannot be updated in this body. Admin edits only TimeIn and TimeOut; backend derives TimeWork and ShiftName from TimeIn. TimeIn and TimeOut must be time only in HH:mm and must be sent together.\nNotes: Admin only. {{workerCode}} must be WorkerCode. If nationality, ShirtType, or ShirtNumber is sent without WorkerCode, backend regenerates WorkerCode/username from nationality + ShirtType + ShirtNumber, using existing profile values for fields omitted from the request. status inactive revokes active worker sessions. Password reset remains separate at PATCH /api/admin/users/{{workerCode}}/password."
          },
          "response": []
        },
        {
          "name": "PATCH /api/admin/users/:workerCode/password",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text",
                "description": "Required for protected routes. Format: Bearer <AccessToken>."
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text",
                "description": "Required when sending JSON body."
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"NewPassword\": \"Worker@654321\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/users/{{workerCode}}/password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "users",
                "{{workerCode}}",
                "password"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: PATCH\nURL: {{baseUrl}}/api/admin/users/{{workerCode}}/password\nAuth: Required via Authorization header\nHeaders: Authorization: Bearer {{accessToken}}, Content-Type: application/json, X-Request-Id (optional, correlation id), X-Client-Version (optional)\nQuery: None\nBody: JSON: NewPassword\nNotes: Admin only. {{workerCode}} must be WorkerCode. Resets worker password and revokes worker sessions."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Admin Jobs",
      "item": [
        {
          "name": "GET /api/admin/jobs/workers/status",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/jobs/workers/status",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "jobs",
                "workers",
                "status"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/jobs/workers/status\nAuth: Admin access token\nBody: None\nNotes: Admin Jobs. Lists active current-shift workers for the worker queue board. Workers with an active queue/assignment remain visible even when their Worker WebSocket is disconnected; use SocketConnected to show the connection state. Response includes FullName, WorkerCode, ShirtNumber, ImageUrl, ShiftName, LatestActivityAt, StatusEnteredAt, QueuePosition, SocketConnected, and one status only. Data is already sorted by board column order and the real order inside each status. Ready workers use Redis QueuePosition. Board status values: open_app, ready, assigned, waiting_team, working, break."
          },
          "response": []
        },
        {
          "name": "POST /api/admin/jobs/workers/:workerCode/status/force",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Status\": \"open_app\",\n  \"ReasonCode\": \"IDLE_TOO_LONG\",\n  \"ReasonText\": \"Worker contacted admin for status correction.\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/jobs/workers/{{workerCode}}/status/force",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "jobs",
                "workers",
                "{{workerCode}}",
                "status",
                "force"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/admin/jobs/workers/{{workerCode}}/status/force\nAuth: Admin access token\nBody: Status open_app|ready|break, ReasonCode required, ReasonText optional. Both are persisted to admin_action_logs (ActionType WORKER_STATUS_FORCED) for audit — VehicleJobId on that log entry is null whenever the worker has no VehicleJob context at the time (the common case: an idle worker sitting in the queue), and only set to the worker's current VehicleJob when one applies (forcing ready on a DELIVERED assignment). Rejects 400 if ReasonCode is missing.\nResponse: Message, FullName, WorkerCode, Status only.\nNotes: Admin Jobs. {{workerCode}} must be WorkerCode and must have an active Worker WebSocket connection for this force action. If the Worker socket is not connected, response is 409 WORKER_NOT_ONLINE. open_app means the Worker opened the app but is not in the ready queue; it removes the Worker from dispatch queue. ready puts the Worker into Redis queue. break starts a configured break, default 15 minutes, and consumes a break count. GET /api/admin/jobs/workers/status can still show active queue or assignment workers with SocketConnected=false when their socket is disconnected."
          },
          "response": []
        },
        {
          "name": "POST /api/admin/vehicle-jobs/assignment/cancel",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"TicketNumber\": \"{{ticketNumber}}\",\n  \"TicketNo\": null,\n  \"BoothCode\": null,\n  \"WorkerCode\": null,\n  \"ReasonCode\": \"ADMIN_CANCEL\",\n  \"ReasonText\": \"Admin cancelled this job.\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/vehicle-jobs/assignment/cancel",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "vehicle-jobs",
                "assignment",
                "cancel"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/admin/vehicle-jobs/assignment/cancel\nAuth: Admin access token with jobs:cancel permission\nReplaces the previous POST /api/admin/jobs/cancel, POST .../tickets/{{ticketNo}}/cancel, POST .../tickets/{{ticketNo}}/stalls/{{stallCode}}/cancel, POST .../workers/{{workerCode}}/assignment/cancel, and POST .../tickets/{{ticketNo}}/workers/{{workerCode}}/cancel endpoints — all removed, use this one instead.\nBody: TicketNumber required. ReasonCode required, never null — always written to admin_action_logs, whichever scope below is resolved; ReasonText optional. TicketNo/BoothCode/WorkerCode optional (send null or omit). Scope is decided purely by which of TicketNo/BoothCode/WorkerCode are present:\nTicketNumber alone -> cancel the whole vehicle job (all workers requeued to front of queue). Only ever cascades to Business Tickets/Booths that are not already COMPLETED/CANCELLED — a booth/market that already finished stays untouched. Rejects 409 VehicleJobClosed if the vehicle is already COMPLETED or CANCELLED (row-locked and re-checked in the same transaction as the cancel, so this also catches a vehicle a concurrent vendor confirmation just closed).\n+ TicketNo -> cancel that one Business Ticket (cascades to every non-terminal booth under it). Rejects 409 MarketJobAlreadyClosed / MarketJobAlreadySubmitted. After cancelling, the parent vehicle job's status is recalculated in the same transaction (same centralized lifecycle vendor confirmation uses) — it becomes COMPLETED if another Business Ticket already completed, CANCELLED if every Business Ticket ended up cancelled, or stays as-is if other Business Tickets are still active.\n+ TicketNo + BoothCode -> cancel that one Booth only, roster untouched. Rejects 409 StallJobAlreadyClosed / StallJobAlreadySubmitted. After cancelling, the parent Business Ticket and vehicle job status are recalculated in the same transaction (same centralized lifecycle vendor confirmation uses) — e.g. if this was the last non-terminal booth and a sibling booth already completed, the Business Ticket finalizes financials and becomes COMPLETED, and the vehicle job follows the same rule as above.\n+ WorkerCode (no TicketNo) -> cancel that one worker's assignment for the whole vehicle job.\n+ TicketNo + WorkerCode (no BoothCode) -> remove that one worker from that one Business Ticket's roster only, worker stays checked in on the truck.\n+ TicketNo + BoothCode + WorkerCode together -> remove that one worker from just that one Booth's payout divisor only (does not touch the worker's roster status at all — they stay WORKING on the rest of the Business Ticket and keep working any other booth in it). Allowed until that booth has ever been submitted (DELIVERED or REJECT), same as full Booth cancellation. Rejects 404 TicketWorkerNotFound if the worker is not an active member of the ticket, 409 StallJobAlreadyClosed/StallJobAlreadySubmitted for the booth's own state, or 409 WorkerAlreadyExcludedFromBooth if already removed from this booth.\nReasonCode/ReasonText traceability: GET .../vehicle-jobs/history Workers[].Cancellation and GET .../vehicle-jobs/history/daily-worker-income Cancellation both read ReasonCode/ReasonText/CancelledByType/CancelledByName from whichever admin_action_logs row actually caused that worker/ticket_no to end up cancelled — including indirectly: a worker/ticket_no cancelled as a side effect of a whole-vehicle cancel (TicketNumber alone) falls back to that VEHICLE_JOB_CANCELLED log, and a ticket_no cancelled as a side effect of its last Booth being cancelled falls back to that STALL_JOB_CANCELLED log, so the Reason you send here always surfaces on those two GET endpoints no matter which scope actually triggered the cancellation.\nExamples:\nWhole vehicle: { \"TicketNumber\": \"{{ticketNumber}}\", \"ReasonCode\": \"ADMIN_CANCEL\", \"ReasonText\": \"Admin cancelled this job.\" }\nOne Business Ticket: { \"TicketNumber\": \"{{ticketNumber}}\", \"TicketNo\": \"{{ticketNo}}\", \"ReasonCode\": \"ADMIN_CANCEL\", \"ReasonText\": \"Cancelled by admin.\" }\nOne Booth: { \"TicketNumber\": \"{{ticketNumber}}\", \"TicketNo\": \"{{ticketNo}}\", \"BoothCode\": \"{{boothCode}}\", \"ReasonCode\": \"ADMIN_CANCEL\", \"ReasonText\": \"Cancelled by admin.\" }\nOne worker, whole vehicle: { \"TicketNumber\": \"{{ticketNumber}}\", \"WorkerCode\": \"{{workerCode}}\", \"ReasonCode\": \"ADMIN_CANCEL\", \"ReasonText\": \"Cancel this worker assignment.\" }\nOne worker, one Business Ticket: { \"TicketNumber\": \"{{ticketNumber}}\", \"TicketNo\": \"{{ticketNo}}\", \"WorkerCode\": \"{{workerCode}}\", \"ReasonCode\": \"ADMIN_CANCEL\", \"ReasonText\": \"Vendor requested a different worker for this ticket only.\" }\nOne worker, one Booth: { \"TicketNumber\": \"{{ticketNumber}}\", \"TicketNo\": \"{{ticketNo}}\", \"BoothCode\": \"{{boothCode}}\", \"WorkerCode\": \"{{workerCode}}\", \"ReasonCode\": \"ADMIN_CANCEL\", \"ReasonText\": \"Worker was reassigned to a different booth.\" }"
          },
          "response": []
        },
        {
          "name": "GET /api/admin/vehicle-jobs/operations",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const body = pm.response.json();",
                  "const firstItem = body.Data?.[0];",
                  "const firstMarket = firstItem?.Markets?.[0];",
                  "const firstTicket = firstMarket?.Tickets?.[0];",
                  "if (firstItem?.VehicleJob?.TicketNo) pm.collectionVariables.set('ticketNo', String(firstItem.VehicleJob.TicketNo));",
                  "if (firstMarket?.MarketCode) pm.collectionVariables.set('marketCode', String(firstMarket.MarketCode));",
                  "if (firstTicket?.BoothCode) { pm.collectionVariables.set('boothCode', String(firstTicket.BoothCode)); pm.collectionVariables.set('stallCode', String(firstTicket.BoothCode)); }",
                  "if (firstTicket?.Products?.[0]?.ProductCode) pm.collectionVariables.set('productCode', String(firstTicket.Products[0].ProductCode));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/vehicle-jobs/operations?date_from=2026-07-01&date_to=2026-07-07&page=1&limit=20&search=&operation_status=&dropoff_point=",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "vehicle-jobs",
                "operations"
              ],
              "query": [
                {
                  "key": "date_from",
                  "value": "2026-07-01",
                  "description": "Optional date range start in YYYY-MM-DD."
                },
                {
                  "key": "date_to",
                  "value": "2026-07-07",
                  "description": "Optional date range end in YYYY-MM-DD, inclusive."
                },
                {
                  "key": "page",
                  "value": "1",
                  "description": "Optional. If omitted, all matching rows are returned."
                },
                {
                  "key": "limit",
                  "value": "20",
                  "description": "Optional page size used only when page is sent."
                },
                {
                  "key": "search",
                  "value": "",
                  "description": "Search TicketNo, license plate, MarketCode/MarketName, BoothCode/BoothName, ProductCode/ProductName, or vendor name."
                },
                {
                  "key": "operation_status",
                  "value": "",
                  "description": "Optional: cancelled, completed, reject, wait_unload, wait_worker, ready_now, working. Mutually exclusive, decided in that priority order. working also covers a vehicle job released early via release-workers (VehicleJob.Status=RELEASED); check VehicleJob.Status directly to isolate released jobs."
                },
                {
                  "key": "dropoff_point",
                  "value": "",
                  "description": "Optional. Filter to vehicles with at least one Market whose DropoffPoint exactly matches this value (case-insensitive). Applied at the DB query level, so summary/pagination reflect the filtered set."
                }
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/vehicle-jobs/operations\nAuth: Admin access token with jobs:read permission\nPurpose: Vehicle operation board for delivery-truck queue/check-in page. Returns ServerTime, summary counts, OperationStatus, workers with ShiftName, scan progress, markets with DropoffPoint, stalls/tickets with RejectReason, and products.\nOperationStatus values (mutually exclusive, decided in this priority order): cancelled, completed, reject (a booth is currently REJECT, unresolved), wait_unload (DispatchNow=false, whether set by Gate or toggled off later by Admin), wait_worker (DispatchNow=true but active workers < WorkersRequired, FIFO auto-dispatch still filling), ready_now (team full and dispatched, but WorkStartedAt is still null), working (WorkStartedAt is set, the whole team has scanned in — also covers a job released early via release-workers, VehicleJob.Status=RELEASED, since the work itself is not done yet; check VehicleJob.Status directly to isolate released jobs). Summary object has the same 7 keys plus Total.\nWorkers[].WorkerStatus values: open_app, assigned, waiting_team, working, released (specific to this board — derived from Workers[].AssignmentStatus=RELEASED, not one of the general worker-status values used by the Worker Board/queue elsewhere). Workers[].ReleasedAt is set once when Admin releases that worker via release-workers, before the whole TicketNumber closes; null until then, never falls back to UpdatedAt/CompletedAt/now.\nVehicleJob.LicensePlate/LicensePlateProvince renamed to PlateNo/PlateProvince (same rename applied to GET /vehicle-jobs/history)."
          },
          "response": []
        },
        {
          "name": "GET /api/admin/vehicle-jobs/history",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const body = pm.response.json();",
                  "const firstItem = body.Data?.[0];",
                  "const firstMarket = firstItem?.Markets?.[0];",
                  "const firstBooth = firstMarket?.Booths?.[0];",
                  "if (firstItem?.VehicleJob?.TicketNumber) pm.collectionVariables.set('ticketNumber', String(firstItem.VehicleJob.TicketNumber));",
                  "if (firstMarket?.MarketCode) pm.collectionVariables.set('marketCode', String(firstMarket.MarketCode));",
                  "if (firstBooth?.BoothCode) { pm.collectionVariables.set('boothCode', String(firstBooth.BoothCode)); pm.collectionVariables.set('stallCode', String(firstBooth.BoothCode)); }",
                  "if (firstBooth?.Products?.[0]?.ProductCode) pm.collectionVariables.set('productCode', String(firstBooth.Products[0].ProductCode));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/vehicle-jobs/history?date_from=2026-07-01&date_to=2026-07-07&page=1&limit=20&search=&status=&dropoff_point=",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "vehicle-jobs",
                "history"
              ],
              "query": [
                {
                  "key": "date_from",
                  "value": "2026-07-01",
                  "description": "Optional date range start in YYYY-MM-DD."
                },
                {
                  "key": "date_to",
                  "value": "2026-07-07",
                  "description": "Optional date range end in YYYY-MM-DD, inclusive."
                },
                {
                  "key": "page",
                  "value": "1",
                  "description": "Optional. If omitted, all matching rows are returned."
                },
                {
                  "key": "limit",
                  "value": "20",
                  "description": "Optional page size used only when page is sent."
                },
                {
                  "key": "search",
                  "value": ""
                },
                {
                  "key": "status",
                  "value": ""
                },
                {
                  "key": "dropoff_point",
                  "value": "",
                  "description": "Optional. Filter to vehicles with at least one Market whose DropoffPoint exactly matches this value (case-insensitive). Applied at the DB query level, so pagination.total reflects the filtered set."
                }
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/vehicle-jobs/history?date_from=2026-07-01&date_to=2026-07-07&page=1&limit=20&search=&status=&dropoff_point=\nAuth: Admin access token\nQuery: date_from/date_to/search/status/dropoff_point optional. date is still supported as a legacy single-day filter. status can match vehicle, market, booth/ticket, confirmation, or worker assignment status. dropoff_point matches Markets[].DropoffPoint exactly (case-insensitive). page optional; when page is omitted backend returns all matching rows ordered by latest CreatedAt. When page is sent backend returns that page and pagination metadata.\nSupported status examples: WAIT, WORKING, DELIVERED, REJECT, COMPLETED, CANCELLED, PENDING, ACCEPTED, SCANNED, TIMEOUT.\nResponse: Data[].VehicleJob plus Markets[].Tickets[].Products[] for the Admin history report. Search can match TicketNo, license plate, MarketCode/MarketName, BoothCode/BoothName, ProductCode/ProductName, or vendor name.\nNotes: Use search with a license plate to see what that vehicle delivered, use MarketCode/MarketName to see vehicles that delivered to that market, or use BoothCode/BoothName to see vehicles and products for that booth in the selected date range.\nUpdated fields: Data[].VehicleJob now also returns WorkStart, SubmittedCompleteAt, VendorConfirmedCompleteAt, CompletedAt, and DurationSeconds (all derived, never stored). Each item also returns Workers[] (every worker ever dispatched to the vehicle job, with AcceptedAt/ScannedAt/StartedAt/SubmittedAt/ReleasedAt/FinalStatus/Cancellation), Timeline[] (chronological merge of worker lifecycle, booth submission/reject/confirm, and admin-action events), and Finance (job-level totals reused from the same persisted financial records as /financials, never recalculated). Markets[].Booths[] (renamed from the older Tickets[] naming) also gained VendorLineId, RejectReason, SubmittedByCodes (renamed from SubmittedWorkerCodes — an Admin submitting on behalf via override-count can now appear here too, alongside Workers), SubmittedAt, ConfirmedAt, and RejectionHistory (each entry's CorrectedByWorkerCode is now split into CorrectedByCode + CorrectedByRole). VehicleJob.LicensePlate/LicensePlateProvince renamed to PlateNo/PlateProvince (same rename applied to GET /vehicle-jobs/operations). Booths[] no longer repeats TicketId/TicketNo/MarketCode/MarketName since those already appear one level up on Markets[], and Booths[].Products[] no longer includes TicketProductId (an internal PK not needed for a read-only report)."
          },
          "response": []
        },
        {
          "name": "GET /api/admin/vehicle-jobs/history/daily-worker-income",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/vehicle-jobs/history/daily-worker-income?date_from=2026-07-01&date_to=2026-07-07&page=1&limit=20&workerCode=&status=&shift=",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "vehicle-jobs",
                "history",
                "daily-worker-income"
              ],
              "query": [
                {
                  "key": "date_from",
                  "value": "2026-07-01",
                  "description": "Optional date range start in YYYY-MM-DD."
                },
                {
                  "key": "date_to",
                  "value": "2026-07-07",
                  "description": "Optional date range end in YYYY-MM-DD, inclusive."
                },
                {
                  "key": "page",
                  "value": "1"
                },
                {
                  "key": "limit",
                  "value": "20"
                },
                {
                  "key": "workerCode",
                  "value": "",
                  "description": "Alias of worker_code. Filters to one worker's rows."
                },
                {
                  "key": "status",
                  "value": "",
                  "description": "Optional filter by TicketWorker status."
                },
                {
                  "key": "shift",
                  "value": "",
                  "description": "Optional filter by the worker's shift number."
                }
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/vehicle-jobs/history/daily-worker-income\nAuth: Admin access token with jobs:read permission\nQuery: date_from/date_to (aliases: from/to), worker_code (alias: workerCode), status, shift, search (alias: keyword), page, limit (alias: pageSize). Nested under the existing /vehicle-jobs/history namespace rather than a new /work-history root, since this report is derived from the same Vehicle Job History data.\nNotes: One row per worker per ticket_no (Business Ticket). Payable is TicketWorker.FinalEarningAmount taken as-is once financialized; never recomputed, already scoped to this one ticket_no. StartedAt is VehicleJob.WorkStartedAt (whole team finished scanning in, same value for every worker on the same truck). SubmittedAt is the latest submission time across every booth in this ticket_no regardless of who submitted, same value for every row of the same ticket_no (like ConfirmedAt). Only rows whose PaymentStatus resolves to one of 5 known values are returned; a row matching none of them (e.g. still in progress with no issue) is excluded from Data and from Pagination.Total entirely.\nResponse: Data[] of {Worker: {Code, Name, Shirt}, AcceptedAt, Shift, TicketNo, Plate, Payable, ScannedAt, StartedAt, SubmittedAt, ConfirmedAt, ReleasedAt, PaymentStatus (success|partially_paid|cancel|admin_reject|worker_reject), Cancellation: {CancelledAt, ReasonCode, ReasonText, CancelledByType, CancelledByName} or null (non-null only when PaymentStatus=cancel, sourced from the admin_action_logs row written when the ticket_no was cancelled)} — no Id field. plus AvailableWorkerCodes[] and AvailableShifts[] (every distinct value under the current date range/search/status filters, independent of each other and of workerCode/shift themselves, always the full list regardless of page/limit — build worker/shift dropdowns directly from these), plus Pagination."
          },
          "response": []
        },
        {
          "name": "GET /api/admin/vehicle-jobs/history/monthly-stall-fees",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/vehicle-jobs/history/monthly-stall-fees?date_from=2026-12-01&date_to=2027-01-31&market_search=&booth_search=&shirt_color=&page=1&limit=20",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "vehicle-jobs",
                "history",
                "monthly-stall-fees"
              ],
              "query": [
                {
                  "key": "date_from",
                  "value": "2026-12-01",
                  "description": "Required date range start in YYYY-MM-DD, interpreted in Asia/Bangkok."
                },
                {
                  "key": "date_to",
                  "value": "2027-01-31",
                  "description": "Required date range end in YYYY-MM-DD, inclusive. May cross months/years freely; must not exceed 6 calendar months from date_from."
                },
                {
                  "key": "market_search",
                  "value": "",
                  "description": "Optional partial match on MarketCode or MarketName."
                },
                {
                  "key": "booth_search",
                  "value": "",
                  "description": "Optional partial match on BoothCode."
                },
                {
                  "key": "shirt_color",
                  "value": "",
                  "description": "Optional exact filter: NAVY, BLUE, GREEN, MIXED, or UNKNOWN."
                },
                {
                  "key": "page",
                  "value": "1"
                },
                {
                  "key": "limit",
                  "value": "20"
                }
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/vehicle-jobs/history/monthly-stall-fees\nAuth: Admin access token with jobs:read permission\nQuery: date_from/date_to (required), market_search, booth_search, shirt_color, page, limit.\nNotes: One row per distinct MarketCode+BoothCode+ShirtColor group, not per TicketProductFinancial like daily-stall-fees. DebitAmount is SUM(TicketProductFinancial.StallFeeRounded) already persisted at finalize time, never recomputed from the current rate. ShirtColor is an immutable snapshot taken at finalize time (TicketProductFinancial.ShirtColorSnapshot), not the worker's current MasterWorker color: NAVY/BLUE/GREEN when every worker on that booth/product matched exactly, MIXED when more than one color was involved, UNKNOWN when there is no snapshot (legacy rows from before this feature existed) or the value on file did not match NAVY/BLUE/GREEN exactly. The totals here must reconcile with the sum of daily-stall-fees over the same date range, since both read the same finalized TicketProductFinancial rows.\nResponse: Period echoing the effective date range, Data[] of {Id, MarketCode, MarketName, BoothCode, ShirtColor, FinancialItemCount, DebitAmount}, Summary ({RowCount, StallCount, FinancialItemCount, DebitAmountTotal} over the full filtered set, independent of page/limit), AvailableMarkets[]/AvailableStalls[]/AvailableShirtColors[] (each faceted: narrowed by the OTHER filters only, never by its own dimension, and independent of page/limit — AvailableStalls entries each carry their own MarketCode), and Pagination."
          },
          "response": []
        },
        {
          "name": "GET /api/admin/vehicle-jobs/:ticketNo/financials",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/vehicle-jobs/{{ticketNo}}/financials",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "vehicle-jobs",
                "{{ticketNo}}",
                "financials"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/vehicle-jobs/{{ticketNo}}/financials\nAuth: Admin access token\nPermission: jobs:read\nBody: None\nResponse: Persisted financial breakdown grouped VehicleJob -> Booth -> Product -> Worker. FinancialStatus is PENDING, PARTIAL, or FINALIZED.\nImportant: This endpoint is read-only. It does not recompute money from current MasterRate. Product financial values come from persisted TicketProductFinancial records and worker earnings come from persisted TicketWorkerPayment.FinalAmount."
          },
          "response": []
        },
        {
          "name": "POST /api/admin/vehicle-jobs/:ticketNo/assign-workers",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const body = pm.response.json();",
                  "if (body.TicketNo) pm.collectionVariables.set('ticketNo', String(body.TicketNo));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"WorkerCodes\": [\n    \"{{workerCode}}\"\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/vehicle-jobs/{{ticketNo}}/assign-workers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "vehicle-jobs",
                "{{ticketNo}}",
                "assign-workers"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/admin/vehicle-jobs/{{ticketNo}}/assign-workers\nAuth: Admin access token\nBody: WorkerCodes array\nNotes: Admin manually assigns the vehicle job to selected workers. Workers must be active, must not already have an active assignment, and must currently be ready in the Redis queue. Backend marks them assigned for UI, removes them from ready queue, and starts the 1-minute accept timeout."
          },
          "response": []
        },
        {
          "name": "POST /api/admin/vehicle-jobs/:ticketNo/scan-deadline/extend",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Minutes\": 10,\n  \"ReasonCode\": \"ADMIN_EXTEND_VEHICLE_ASSIGNMENT_SCAN_TIMER\",\n  \"ReasonText\": \"Worker needs more time to reach QR scan point.\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/vehicle-jobs/{{ticketNo}}/scan-deadline/extend",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "vehicle-jobs",
                "{{ticketNo}}",
                "scan-deadline",
                "extend"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/admin/vehicle-jobs/{{ticketNo}}/scan-deadline/extend\nAuth: Admin access token\nBody: Minutes required. ReasonCode required, ReasonText optional — both persisted to admin_action_logs (ActionType SCAN_DEADLINE_EXTENDED) for audit; rejects 400 if ReasonCode is missing. Omit WorkerCodes to extend all ACCEPTED workers whose scan deadline has not expired; send WorkerCodes to extend selected workers only.\nExample selected worker body: { \"Minutes\": 10, \"WorkerCodes\": [\"MN000115\"], \"ReasonCode\": \"ADMIN_EXTEND_VEHICLE_ASSIGNMENT_SCAN_TIMER\" }\nResponse: ticketNo, assignments[].WorkerCode/status/ScanDeadlineAt/ScanDeadlineUnixMs.\nNotes: Extends ScanDeadlineAt and ScanDeadlineUnixMs, then reschedules the background scan timeout for the updated ACCEPTED assignments. Expired assignments are not extended."
          },
          "response": []
        },
        {
          "name": "POST /api/admin/vehicle-jobs/:ticketNumber/tickets/:ticketNo/stalls/:stallCode/override-count",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"ReasonCode\": \"R001\",\n  \"ReasonText\": \"Worker entered the wrong count.\",\n  \"Counts\": [\n    {\n      \"ProductCode\": \"{{productCode}}\",\n      \"PackageCode\": \"{{packageCode}}\",\n      \"ActualQuantity\": 7\n    }\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/vehicle-jobs/{{ticketNumber}}/tickets/{{ticketNo}}/stalls/{{stallCode}}/override-count",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "vehicle-jobs",
                "{{ticketNumber}}",
                "tickets",
                "{{ticketNo}}",
                "stalls",
                "{{stallCode}}",
                "override-count"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/admin/vehicle-jobs/{{ticketNumber}}/tickets/{{ticketNo}}/stalls/{{stallCode}}/override-count\nAuth: Admin access token with jobs:override_count permission\nBody: ReasonCode required, ReasonText optional, Counts[] of {ProductCode, PackageCode, ActualQuantity}. Counts must include every product on the booth.\nResponse: Message, TicketNumber, BoothCode, Status, ReasonCode, ReasonText, Products[]."
          },
          "response": []
        },
        {
          "name": "POST /api/admin/vehicle-jobs/:ticketNumber/wait",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Dispatch\": false,\n  \"ReasonCode\": \"DRIVER_NOT_READY\",\n  \"ReasonText\": \"Driver requested more time before unloading.\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/vehicle-jobs/{{ticketNumber}}/wait",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "vehicle-jobs",
                "{{ticketNumber}}",
                "wait"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/admin/vehicle-jobs/{{ticketNumber}}/wait\nAuth: Admin access token with jobs:wait permission\nBody: Dispatch (required boolean), ReasonCode (required, non-empty), ReasonText (optional — may be omitted or sent as null)\nNotes: Toggles the vehicle job's dispatch. Dispatch=false puts it back to \"waiting to unload\" (DispatchNow=false, Status=WAIT): cancels every currently active VehicleJobAssignment for the team and puts the whole team back at the front of the FIFO queue, then runs a dispatch pass so those freed workers can pick up other already-dispatched vehicle jobs immediately. Dispatch=true puts it back to dispatched (DispatchNow=true, Status=WORKING) and calls workers from the queue for this vehicle job right away — same mechanics as Gate creating a job with Dispatch=true; the team pulled in may be a different one, depending on who is at the front of the queue at that moment. Either direction is only allowed while the whole required team has NOT YET finished checking in (still PENDING/ACCEPTED/partially SCANNED — \"wait_team\"); once every required worker has scanned in, dispatch can no longer be changed (409 VEHICLE_JOB_ALREADY_STARTED), since no booth could still be un-started past that point. Returns 409 VEHICLE_JOB_CLOSED if the vehicle job is already COMPLETED/CANCELLED. Recorded in AdminActionLog (Metadata.Dispatch records the direction) and appears in GET /api/admin/vehicle-jobs/history Timeline as an ADMIN_ACTION event.\nResponse: Message, TicketNumber, Status, DispatchNow, RequeuedWorkerCodes[] (only populated when Dispatch=false), ReasonCode, ReasonText."
          },
          "response": []
        },
        {
          "name": "POST /api/admin/vehicle-jobs/:ticketNumber/release-workers",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"ReasonCode\": \"ALL_BOOTHS_RESOLVED\",\n  \"ReasonText\": \"All booths submitted, no unresolved rejection.\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/vehicle-jobs/{{ticketNumber}}/release-workers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "vehicle-jobs",
                "{{ticketNumber}}",
                "release-workers"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/admin/vehicle-jobs/{{ticketNumber}}/release-workers\nAuth: Admin access token with jobs:release_workers permission\nBody: ReasonCode (required), ReasonText (optional)\nNotes: Releases the dispatched worker team back to the FIFO queue once every booth has been submitted — status DELIVERED (submitted, awaiting vendor confirmation), COMPLETED (vendor confirmed), or CANCELLED. Does NOT require vendor confirmation first: the worker's physical duty ends the moment they submit counted quantities, so the team can be released while a booth is still DELIVERED and waiting on the vendor — if the vendor later rejects, the Worker (or Admin via override-count) can still resubmit regardless of whether the original team was released. Also does not wait for the whole vehicle job to close, Gate to signal no more Business Tickets, or per-Business-Ticket financial finalize — money is settled later independently. Booth/ticket status is unaffected (stays DELIVERED/WORKING in Work History until the vendor actually confirms or times out), but the VehicleJob's own status moves from WORKING to RELEASED (non-terminal) so it is excluded from FIFO dispatch until Gate submits a new booth for this ticketNumber (reopens dispatch automatically, same as from WAIT) or Admin reopens dispatch via /wait — this prevents the just-released team, or any other queued worker, from being immediately redispatched back onto the same vehicle job. Workers are appended to the BACK of the FIFO queue ordered by AcceptedAt (earliest accept first), the same relative ordering used when they first joined the queue — not by the order their VehicleJobAssignment rows were created/dispatched. Returns 409 if the vehicle job is already closed (VEHICLE_JOB_CLOSED), has no booths yet (NO_SUBMITTED_BOOTHS), has a booth still WAIT/WORKING/REJECT — REJECT means an unresolved vendor rejection the worker must fix and resubmit first (BOOTHS_NOT_SUBMITTED), or has no releasable workers (NO_RELEASABLE_WORKERS). Recorded in AdminActionLog and appears in GET /api/admin/vehicle-jobs/history Timeline as an ADMIN_ACTION event plus a WORKER_RELEASED event per worker.\nResponse: Message, TicketNumber, ReleasedWorkerCodes[] (ordered by AcceptedAt), ReasonCode, ReasonText."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Admin Audit",
      "item": [
        {
          "name": "Worker Performance - Today",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "description": "Admin Audit worker performance for today using Asia/Bangkok business date.",
            "url": {
              "raw": "{{baseUrl}}/api/admin/audit/workers/performance",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "audit",
                "workers",
                "performance"
              ]
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Worker Performance - Worker Today",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "description": "Admin Audit worker performance for one worker today.",
            "url": {
              "raw": "{{baseUrl}}/api/admin/audit/workers/performance?worker_code=001",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "audit",
                "workers",
                "performance"
              ],
              "query": [
                {
                  "key": "worker_code",
                  "value": "001"
                }
              ]
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Worker Performance - Date Range",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "description": "Admin Audit worker performance for a Bangkok date range.",
            "url": {
              "raw": "{{baseUrl}}/api/admin/audit/workers/performance?date_from=2026-08-01&date_to=2026-08-31",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "audit",
                "workers",
                "performance"
              ],
              "query": [
                {
                  "key": "date_from",
                  "value": "2026-08-01"
                },
                {
                  "key": "date_to",
                  "value": "2026-08-31"
                }
              ]
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        },
        {
          "name": "Worker Performance - Worker Date Range",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "description": "Admin Audit worker performance for one worker in a Bangkok date range.",
            "url": {
              "raw": "{{baseUrl}}/api/admin/audit/workers/performance?worker_code=001&date_from=2026-08-01&date_to=2026-08-31",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "audit",
                "workers",
                "performance"
              ],
              "query": [
                {
                  "key": "worker_code",
                  "value": "001"
                },
                {
                  "key": "date_from",
                  "value": "2026-08-01"
                },
                {
                  "key": "date_to",
                  "value": "2026-08-31"
                }
              ]
            },
            "auth": {
              "type": "noauth"
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "Admin Settings",
      "item": [
        {
          "name": "GET /api/admin/settings",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/settings",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "settings"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/settings\nAuth: Admin access token with settings:read permission\nBody: None\nNotes: Returns runtime settings used by backend calculations."
          },
          "response": []
        },
        {
          "name": "PATCH /api/admin/settings",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"DriverSessionTtlHours\": 24,\n  \"WorkerAcceptDeadlineSeconds\": 60,\n  \"WorkerAcceptTimeoutLimit\": 3,\n  \"WorkerScanDeadlineMinutes\": 15,\n  \"WorkerScanWarningBeforeMinutes\": 2,\n  \"WorkerScanTeamRemainingMinutes\": 5,\n  \"WorkerBreakDurationMinutes\": 15,\n  \"WorkerBreakLimit\": 4,\n  \"WorkerBreakCountTtlHours\": 48,\n  \"WorkerPresenceStaleSeconds\": 90,\n  \"VendorConfirmTimeoutHours\": 24,\n  \"VendorReconfirmTimeoutHours\": 4\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/settings",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "settings"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: PATCH\nURL: {{baseUrl}}/api/admin/settings\nAuth: Admin access token with settings:update permission\nBody: Partial JSON is allowed. Updated settings affect new calculations after this request; existing assignment deadlines already saved in DB remain unchanged. WorkerAcceptTimeoutLimit controls how many consecutive accept timeouts are allowed before the worker is moved to open_app and the current shift is closed. WorkerScanWarningBeforeMinutes controls when Admin SSE receives ASSIGNMENT_SCAN_DEADLINE_WARNING for ACCEPTED workers who have not checked in. WorkerScanTeamRemainingMinutes controls the shortened scan window for remaining team members after the first worker checks in. VendorConfirmTimeoutHours auto-confirms first submitted totals after vendor timeout, and VendorReconfirmTimeoutHours does the same after a rejected ticket is resubmitted."
          },
          "response": []
        },
        {
          "name": "GET /api/admin/mobile-app-versions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/mobile-app-versions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "mobile-app-versions"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/mobile-app-versions\nAuth: Admin access token with settings:read permission\nBody: None\nNotes: Returns Current, Scheduled, and History Worker Mobile Application versions. Current/Scheduled/History are derived from BuildNumber + ForceUpdateAt compared against server time on every call, not a stored status. History never deletes old versions."
          },
          "response": []
        },
        {
          "name": "POST /api/admin/mobile-app-versions",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const body = pm.response.json();",
                  "if (body.Id) pm.collectionVariables.set('mobileAppVersionId', String(body.Id));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Version\": \"1.4.0\",\n  \"BuildNumber\": 10400,\n  \"ReleaseAt\": \"2026-08-19T10:00:00.000Z\",\n  \"AndroidDownloadUrl\": \"https://play.google.com/store/apps/details?id=com.example.worker\",\n  \"IosDownloadUrl\": \"https://apps.apple.com/app/id0000000000\",\n  \"ReleaseMessage\": \"กรุณาอัปเดตแอปแรงงานตามเวลาที่กำหนด\",\n  \"ReleaseNotes\": \"ปรับปรุงระบบรับงานและการแจ้งเตือน\",\n  \"ForceUpdateAt\": \"2026-08-22T15:00:00.000Z\",\n  \"ReleaseNotificationAt\": \"2026-08-20T02:00:00.000Z\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/mobile-app-versions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "mobile-app-versions"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/admin/mobile-app-versions\nAuth: Admin access token with settings:update permission\nBody: Version and BuildNumber required (BuildNumber must be unique, it is the sole comparison key for Worker app version checks — never the Version string). There is no separate ForceUpdate flag: whether a version is forced is derived purely from whether ForceUpdateAt is set.\nNotes: Creating a version never affects the Worker app immediately by itself. If ForceUpdateAt is set and in the future, the new version is Scheduled and GET /api/workers/app-version/check keeps returning the current version until server time reaches ForceUpdateAt — there is no separate Publish endpoint. If ForceUpdateAt is omitted, the new version takes effect immediately and is never forced.\n\nTwo independent FCM notifications are involved, each with its own timing field and its own \"already sent\" tracker: the ReleaseMessage notice fires at ReleaseNotificationAt (omit/null = send synchronously in this request and set ReleaseNotificationSentAt; a future time schedules it via BullMQ instead, and must not be later than ForceUpdateAt); the force-update notice is a standard-pattern-only FCM that fires automatically at ForceUpdateAt itself, tracked independently via ForceUpdateNotificationSentAt, and only scheduled when ForceUpdateAt is set. Neither notification ever activates the version early. ReleaseAt is metadata only (UI/History display), not a trigger for anything. Response Id is saved to {{mobileAppVersionId}} for the PATCH request below."
          },
          "response": []
        },
        {
          "name": "PATCH /api/admin/mobile-app-versions/:id",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"ForceUpdateAt\": \"2026-08-27T15:00:00.000Z\",\n  \"ReleaseNotificationAt\": \"2026-08-26T09:00:00.000Z\",\n  \"ReleaseMessage\": \"กรุณาอัปเดตแอปแรงงานภายในเวลาที่กำหนดใหม่\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/mobile-app-versions/{{mobileAppVersionId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "mobile-app-versions",
                "{{mobileAppVersionId}}"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: PATCH\nURL: {{baseUrl}}/api/admin/mobile-app-versions/{{mobileAppVersionId}}\nAuth: Admin access token with settings:update permission\nBody: At least one field required. Version/BuildNumber can only be changed while the version is still Scheduled (409 MOBILE_APP_VERSION_LOCKED once it becomes Current/History, since changing them afterward would corrupt History). Other fields (URLs, release text, ForceUpdateAt, ReleaseNotificationAt) can always be changed; both ForceUpdateAt and ReleaseNotificationAt accept null to clear them.\nNotes: Changing ForceUpdateAt on a Scheduled version just changes when it activates (or removes the force entirely, if set to null) — Worker keeps using the current version until the new time is reached; version activation is derived live from server time and never depends on either notification job. Changing ForceUpdateAt/ReleaseNotificationAt while their respective ForceUpdateNotificationSentAt/ReleaseNotificationSentAt is still null removes any existing delayed job and re-syncs it (reschedules at the new time, or sends immediately if set to null). Once sent, further edits to these fields only update the stored value — neither notification is ever resent automatically."
          },
          "response": []
        },
        {
          "name": "GET /api/admin/roles",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/roles",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "roles"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/roles\nAuth: Admin access token with roles:read permission\nBody: None\nNotes: Lists admin permission levels ordered from highest to lowest and includes admin accounts grouped inside each role. Permissions are stored per admin account in DB, not derived from this response."
          },
          "response": []
        },
        {
          "name": "GET /api/admin/users/:id/permissions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/users/{{adminUserId}}/permissions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "users",
                "{{adminUserId}}",
                "permissions"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/users/{{adminUserId}}/permissions\nAuth: Admin access token with permissions:read permission\nBody: None\nNotes: Use an admin account id, not a worker id. The target admin must have a lower PermissionLevel than the actor. Use GET /api/auth/me to see your own permissions."
          },
          "response": []
        },
        {
          "name": "PATCH /api/admin/users/:id/permissions",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"PermissionLevel\": \"supervisor\",\n  \"Status\": \"active\",\n  \"Permissions\": [\n    \"settings:read\",\n    \"roles:read\",\n    \"gate_clients:read\",\n    \"gate_clients:create\",\n    \"gate_clients:update\",\n    \"gate_clients:rotate_secret\",\n    \"permissions:read\",\n    \"workers:read\",\n    \"workers:create\",\n    \"workers:update\",\n    \"workers:reset_password\",\n    \"workers:force_status\",\n    \"jobs:read\",\n    \"jobs:assign\",\n    \"jobs:cancel\",\n    \"jobs:extend_deadline\"\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/users/{{adminUserId}}/permissions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "users",
                "{{adminUserId}}",
                "permissions"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: PATCH\nURL: {{baseUrl}}/api/admin/users/{{adminUserId}}/permissions\nAuth: Admin access token with permissions:update permission\nBody: PermissionLevel, optional status active|inactive, and permissions array\nNotes: Updates admin PermissionLevel, status, and permissions, then revokes active sessions of that admin account. Permission is stored per account from DB. The actor must have a higher PermissionLevel rank than both the target admin and the new PermissionLevel. The target admin must login again to receive the latest permissions/status in the token."
          },
          "response": []
        },
        {
          "name": "GET /api/admin/gate-clients",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/gate-clients",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "gate-clients"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/gate-clients\nAuth: Admin access token with gate_clients:read permission\nBody: None\nNotes: Lists Gate clients for system-to-system authentication. Response never includes ClientSecret or SecretHash."
          },
          "response": []
        },
        {
          "name": "PATCH /api/admin/gate-clients/:clientId",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Name\": \"Main Gate\",\n  \"Status\": \"active\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/gate-clients/{{gateClientId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "gate-clients",
                "{{gateClientId}}"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: PATCH\nURL: {{baseUrl}}/api/admin/gate-clients/{{gateClientId}}\nAuth: Admin access token with gate_clients:update permission\nBody: name and/or status active|inactive\nNotes: Activate/disable a Gate client without exposing or changing its secret."
          },
          "response": []
        },
        {
          "name": "POST /api/admin/admins",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const body = pm.response.json();",
                  "if (body.Account?.Id) pm.collectionVariables.set('adminUserId', String(body.Account.Id));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Username\": \"manager01\",\n  \"Password\": \"Manager@123456\",\n  \"FullName\": \"Branch Manager\",\n  \"Position\": \"Manager\",\n  \"Email\": \"manager@simmummuang.local\",\n  \"Phone\": \"081-000-0002\",\n  \"PermissionLevel\": \"manager\",\n  \"Permissions\": [\n    \"settings:read\",\n    \"roles:read\",\n    \"gate_clients:read\",\n    \"gate_clients:create\",\n    \"gate_clients:update\",\n    \"gate_clients:rotate_secret\",\n    \"permissions:read\",\n    \"workers:read\",\n    \"workers:create\",\n    \"workers:update\",\n    \"workers:reset_password\",\n    \"workers:force_status\",\n    \"jobs:read\",\n    \"jobs:assign\",\n    \"jobs:cancel\",\n    \"jobs:extend_deadline\"\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/admins",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "admins"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/admin/admins\nAuth: Admin access token with admins:create permission\nBody: username, password, FullName, optional position/email/phone/status, PermissionLevel owner|manager|supervisor, and permissions array\nNotes: Creates role=admin only and saves email/phone to accounts.email/accounts.phone when provided. Seed owner admin can create manager/supervisor. Manager can create supervisor only. No API can create owner because the actor must have a higher PermissionLevel than the new account. Saves created account id to {{adminUserId}}."
          },
          "response": []
        },
        {
          "name": "POST /api/admin/gate-clients",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const body = pm.response.json();",
                  "if (body.ClientId) pm.collectionVariables.set('gateClientId', String(body.ClientId));",
                  "if (body.ClientSecret) pm.collectionVariables.set('gateClientSecret', String(body.ClientSecret));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"ClientId\": \"gate-main\",\n  \"Name\": \"Main Gate\",\n  \"Status\": \"active\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/admin/gate-clients",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "gate-clients"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/admin/gate-clients\nAuth: Admin access token with gate_clients:create permission\nBody: name required, optional ClientId and status active|inactive\nNotes: Creates a Gate client credential. ClientSecret is shown only once in this response and is saved to {{gateClientSecret}} for Postman testing. Backend stores only SecretHash in DB."
          },
          "response": []
        },
        {
          "name": "POST /api/admin/gate-clients/:clientId/secret/rotate",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const body = pm.response.json();",
                  "if (body.ClientSecret) pm.collectionVariables.set('gateClientSecret', String(body.ClientSecret));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/gate-clients/{{gateClientId}}/secret/rotate",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "gate-clients",
                "{{gateClientId}}",
                "secret",
                "rotate"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/admin/gate-clients/{{gateClientId}}/secret/rotate\nAuth: Admin access token with gate_clients:rotate_secret permission\nBody: None\nNotes: Generates a new ClientSecret for the same ClientId and replaces the stored SecretHash. The old secret stops working immediately. The new secret is shown only once and saved to {{gateClientSecret}}."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Admin Realtime",
      "item": [
        {
          "name": "GET /api/admin/events",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Accept",
                "value": "text/event-stream",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/admin/events",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "admin",
                "events"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/admin/events\nAuth: Admin access token\nPermission: jobs:read\nHeaders: Authorization: Bearer {{accessToken}}, Accept: text/event-stream, X-Request-Id (optional, correlation id), X-Client-Version (optional)\nBody: None\nNotes: Admin-only SSE stream. It stays open and receives realtime dashboard events. Snapshot/latest state should still be loaded from Admin REST APIs."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Gate Flow",
      "item": [
        {
          "name": "GET /api/gate/options",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', function () {",
                  "  pm.response.to.have.status(200);",
                  "});",
                  "",
                  "const body = pm.response.json();",
                  "pm.test('Gate options contains Markets, Booths, Products', function () {",
                  "  pm.expect(body.Markets).to.be.an('array');",
                  "  pm.expect(body.Booths).to.be.an('array');",
                  "  pm.expect(body.Products).to.be.an('array');",
                  "});",
                  "",
                  "const forbiddenKeys = new Set([",
                  "  'StallRate', 'LaborRate', 'PackagePrice', 'PackageRate',",
                  "  'StallPayment', 'WorkerPayment', 'OrderRemainder',",
                  "  'FinalStallAmount', 'FundAmount'",
                  "]);",
                  "",
                  "function findForbidden(value, path = 'root', found = []) {",
                  "  if (Array.isArray(value)) {",
                  "    value.forEach((item, index) => findForbidden(item, `${path}[${index}]`, found));",
                  "    return found;",
                  "  }",
                  "  if (value && typeof value === 'object') {",
                  "    Object.entries(value).forEach(([key, child]) => {",
                  "      if (forbiddenKeys.has(key)) found.push(`${path}.${key}`);",
                  "      findForbidden(child, `${path}.${key}`, found);",
                  "    });",
                  "  }",
                  "  return found;",
                  "}",
                  "",
                  "pm.test('Gate options does not expose financial rates or amounts', function () {",
                  "  pm.expect(findForbidden(body)).to.eql([]);",
                  "});",
                  "",
                  "const firstMarket = body.Markets?.[0];",
                  "const firstBooth = body.Booths?.[0];",
                  "const firstProduct = body.Products?.[0];",
                  "const firstPackage = firstProduct?.Packages?.[0];",
                  "if (firstMarket?.MarketCode) pm.collectionVariables.set('marketCode', firstMarket.MarketCode);",
                  "if (firstBooth?.BoothCode) { pm.collectionVariables.set('boothCode', firstBooth.BoothCode); pm.collectionVariables.set('stallCode', firstBooth.BoothCode); }",
                  "if (firstProduct?.ProductCode) pm.collectionVariables.set('productCode', firstProduct.ProductCode);",
                  "if (firstPackage?.PackageCode) pm.collectionVariables.set('packageCode', firstPackage.PackageCode);"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/gate/options",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "gate",
                "options"
              ]
            },
            "auth": {
              "type": "basic",
              "basic": [
                {
                  "key": "username",
                  "value": "{{gateClientId}}",
                  "type": "string"
                },
                {
                  "key": "password",
                  "value": "{{gateClientSecret}}",
                  "type": "string"
                }
              ]
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/gate/options\nAuth: Gate Basic Auth using {{gateClientId}} / {{gateClientSecret}}.\nBody: None\nPurpose: Test helper for Postman / Swagger / Gate integration testing.\nResponse: Markets, Booths, Products, and Packages with PackageWeight only. This endpoint must not expose financial rates or calculated money.\nNotes: The test script stores the first MarketCode, BoothCode, ProductCode, and PackageCode into collection variables for follow-up requests."
          },
          "response": []
        },
        {
          "name": "POST /api/gate/tickets",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const body = pm.response.json();",
                  "const firstBooth = body.Booths?.[0];",
                  "const firstProduct = firstBooth?.Products?.[0];",
                  "",
                  "pm.test('Gate response is operational only', function () {",
                  "  pm.expect(body).to.have.property('Result');",
                  "  pm.expect(body).to.have.property('Ticket');",
                  "  pm.expect(body).to.have.property('Market');",
                  "  pm.expect(body.Booths).to.be.an('array');",
                  "  pm.expect(body).to.have.property('WorkerCount');",
                  "  pm.expect(body).to.have.property('Qr');",
                  "  pm.expect(body).to.not.have.property('WorkerPayment');",
                  "  pm.expect(body).to.not.have.property('OrderRemainder');",
                  "});",
                  "",
                  "pm.test('Gate booth response does not expose StallPayment', function () {",
                  "  (body.Booths || []).forEach((booth) => pm.expect(booth).to.not.have.property('StallPayment'));",
                  "});",
                  "",
                  "if (body.Result) pm.collectionVariables.set('gateResult', body.Result);",
                  "if (body.TicketNumber) pm.collectionVariables.set('ticketNumber', body.TicketNumber);",
                  "if (body.Ticket?.TicketNo) pm.collectionVariables.set('ticketNo', body.Ticket.TicketNo);",
                  "if (body.Market?.MarketCode) pm.collectionVariables.set('marketCode', body.Market.MarketCode);",
                  "if (body.Market?.DropoffPoint) pm.collectionVariables.set('dropoffPoint', body.Market.DropoffPoint);",
                  "if (firstBooth?.BoothCode) { pm.collectionVariables.set('boothCode', firstBooth.BoothCode); pm.collectionVariables.set('stallCode', firstBooth.BoothCode); }",
                  "if (firstProduct?.ProductCode) pm.collectionVariables.set('productCode', firstProduct.ProductCode);",
                  "if (firstProduct?.PackageCode) pm.collectionVariables.set('packageCode', firstProduct.PackageCode);",
                  "if (body.WorkerCount !== undefined) pm.collectionVariables.set('workerCount', String(body.WorkerCount));",
                  "if (body.Qr?.DriverQrToken) pm.collectionVariables.set('driverQrToken', body.Qr.DriverQrToken);"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"TicketNumber\": \"20260804000001\",\n  \"TicketNo\": \"20260804000002\",\n  \"TicketCreatedAt\": \"2026-08-04T14:30:00+07:00\",\n  \"BoothCount\": 2,\n  \"MarketCode\": \"1123\",\n  \"DropoffPoint\": \"Gate 1 - Zone A\",\n  \"LicensePlate\": \"ABC-1234\",\n  \"LicensePlateProvince\": \"Bangkok\",\n  \"VehicleTypeCode\": \"PICKUP\",\n  \"VehicleTypeName\": \"Pickup truck\",\n  \"Booths\": [\n    {\n      \"BoothCode\": \"OM3/40\",\n      \"Products\": [\n        {\n          \"ProductCode\": \"02020300\",\n          \"PackageCode\": \"29\",\n          \"Quantity\": 180\n        }\n      ]\n    },\n    {\n      \"BoothCode\": \"OM1/1\",\n      \"Products\": [\n        {\n          \"ProductCode\": \"02030103\",\n          \"PackageCode\": \"19\",\n          \"Quantity\": 100\n        },\n        {\n          \"ProductCode\": \"02011701\",\n          \"PackageCode\": \"19\",\n          \"Quantity\": 80\n        }\n      ]\n    }\n  ],\n  \"Dispatch\": true\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/gate/tickets",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "gate",
                "tickets"
              ]
            },
            "auth": {
              "type": "basic",
              "basic": [
                {
                  "key": "username",
                  "value": "{{gateClientId}}",
                  "type": "string"
                },
                {
                  "key": "password",
                  "value": "{{gateClientSecret}}",
                  "type": "string"
                }
              ]
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/gate/tickets\nAuth: Gate Basic Auth using {{gateClientId}} / {{gateClientSecret}}. Create credentials with Admin Settings > POST /api/admin/gate-clients first.\nHeaders: Content-Type: application/json, X-Request-Id (optional, correlation id), X-Client-Version (optional)\nBody: One request per Business Ticket (TicketNo), always against exactly one MarketCode. TicketNumber and TicketNo must both be exactly 14 digits, numbers only. A vehicle (TicketNumber) may generate multiple Business Tickets over time as it unloads at different markets — send the same TicketNumber again with a new TicketNo; no duplicate vehicle job is created. TicketNo is scoped to one market: sending the same TicketNo again under the same TicketNumber and the same MarketCode (while that Ticket is still active) appends the new Booths/Products into the existing Business Ticket instead of creating a new one. Appending a BoothCode that already exists in that Ticket is rejected with GATE_BOOTH_ALREADY_EXISTS_IN_TICKET, and appending after that Ticket's worker roster is already locked is rejected with GATE_TICKET_ROSTER_LOCKED. Sending the same TicketNo again for a *different* MarketCode is always rejected with GATE_TICKET_ALREADY_EXISTS. If a Ticket was created with wrong data (wrong market, needs a full redo), an Admin must cancel it first (POST /api/admin/vehicle-jobs/assignment/cancel with TicketNumber+TicketNo); once cancelled, that exact TicketNo becomes free again and Gate can resend it under the same TicketNumber (any MarketCode) to create a brand-new Business Ticket — the cancelled Ticket is kept untouched as history. WorkerCount for a Ticket is the MAX (not sum) across every product in every booth of that Ticket, recomputed as the MAX between the Ticket's current value and this request's own MAX. Required root fields: TicketNumber, TicketNo, TicketCreatedAt, BoothCount, MarketCode, LicensePlate, LicensePlateProvince, VehicleTypeCode, VehicleTypeName, Booths, Dispatch. Gate does not send a ticket count — the number of active Business Tickets under a TicketNumber is simply however many active markets have a Ticket. Each Booth requires BoothCode and Products. Each Product requires ProductCode, PackageCode, Quantity. BoothCount must equal Booths.length. BoothCode cannot be duplicated in the same request. ProductCode + PackageCode cannot be duplicated inside the same booth.\nFinancial boundary: Gate Quantity is operational/dispatch quantity only. The backend may snapshot the applicable Master rate internally at Gate Create, but Gate Create does not calculate or expose StallPayment, WorkerPayment, OrderRemainder, Fund, or final financial amounts. Financialization happens only after worker-confirmed quantities are vendor-confirmed or auto-confirmed.\nResponse: Result, TicketNumber, Ticket, Market, Booths with operational product data, WorkerCount for dispatch, and Qr (DriverQrToken only, shared across the whole vehicle). Workers check in by scanning the barcode already printed on the physical Gate paper ticket (Ticket.TicketNo) — no separate QR field is generated. Ticket.Status is derived from Dispatch only: true = unload_now, false = waiting_unload.\nIdempotency: Gate transaction ref is derived from the full request payload (not just TicketNumber+TicketNo), because the same TicketNumber+TicketNo+MarketCode can legitimately be sent again with different content to append. Same payload returns REPLAYED. Same TicketNumber+TicketNo+MarketCode with different content is appended rather than rejected.\nVehicle closing: since Gate never signals \"more Tickets coming,\" intake for a vehicle closes automatically as soon as its first Business Ticket is created. Once every known Business Ticket's booths are terminal, the vehicle job completes and its workers return to queue automatically. There is no separate close endpoint.\nNotes: Saves gateResult, ticketNumber, ticketNo, marketCode, first BoothCode, first ProductCode, first PackageCode, workerCount, and driverQrToken into collection variables."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Driver Flow",
      "item": [
        {
          "name": "POST /api/driver/qr-sessions",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const body = pm.response.json();",
                  "if (body.DriverSessionToken) pm.collectionVariables.set('driverSessionToken', body.DriverSessionToken);",
                  "if (body.VehicleJob?.TicketNo) pm.collectionVariables.set('ticketNo', String(body.VehicleJob.TicketNo));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"QrToken\": \"{{driverQrToken}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/driver/qr-sessions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "driver",
                "qr-sessions"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/driver/qr-sessions\nAuth: No auth required\nBody: JSON QrToken from driver QR\nNotes: Creates a 24-hour driver guest session and saves driverSessionToken."
          },
          "response": []
        },
        {
          "name": "GET /api/driver/jobs/current",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{driverSessionToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/driver/jobs/current",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "driver",
                "jobs",
                "current"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/driver/jobs/current\nAuth: Driver guest session via Authorization: Bearer {{driverSessionToken}}\nBody: None"
          },
          "response": []
        },
        {
          "name": "POST /api/driver/jobs/:ticketNo/ready",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{driverSessionToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/driver/jobs/{{ticketNo}}/ready",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "driver",
                "jobs",
                "{{ticketNo}}",
                "ready"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/driver/jobs/{{ticketNo}}/ready\nAuth: Driver guest session\nBody: None\nResponse: ticketNo, LicensePlate, LicensePlateProvince, status.\nNotes: Marks vehicle job WORKING and dispatches ready workers from Redis FIFO queue. Workers check in separately by scanning each Business Ticket's own barcode via POST /api/workers/me/assignments/check-in-barcode."
          },
          "response": []
        }
      ]
    },
    {
      "name": "Worker Application",
      "description": "WebSocket setup: Postman HTTP request items cannot open ws:// as a native WebSocket tab. Create it manually with New > WebSocket, then use {{wsBaseUrl}}/ws/workers?token={{accessToken}} and click Connect. Do this after POST /api/auth/login - Worker and before POST /api/workers/me/online. WORKER_CONNECTED payload returns WorkerCode, not internal account_id. The socket only confirms the worker realtime connection is alive; the worker becomes online/ready after POST /api/workers/me/online.",
      "item": [
        {
          "name": "GET Worker App Version Check",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/workers/app-version/check?platform=android&version=1.3.0&build_number=10300",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "workers",
                "app-version",
                "check"
              ],
              "query": [
                {
                  "key": "platform",
                  "value": "android"
                },
                {
                  "key": "version",
                  "value": "1.3.0"
                },
                {
                  "key": "build_number",
                  "value": "10300"
                }
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/workers/app-version/check\nAuth: Public — no Bearer token, no Worker session/login required, since the app must be able to check its version before the worker logs in.\nQuery: platform (android|ios) selects which DownloadUrl to return; version is informational only; build_number is the worker app's own build number and the only value actually compared (never the Version string).\nNotes: Version management (create/list/update) is documented under the Admin Settings folder — POST/GET/PATCH /api/admin/mobile-app-versions. Server time is always the source of truth — a Scheduled version never becomes effective before its ForceUpdateAt, and a client BuildNumber equal to or higher than the effective BuildNumber always returns UP_TO_DATE (never asks to downgrade). Status is one of UP_TO_DATE, UPDATE_AVAILABLE, UPDATE_REQUIRED, NO_VERSION_CONFIGURED."
          },
          "response": []
        },
        {
          "name": "GET /api/workers/me/status",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/workers/me/status",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "workers",
                "me",
                "status"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/workers/me/status\nAuth: Worker access token\nBody: None\nSafe to call any time, including right after the app is closed and reopened, since CurrentJob is always re-derived live from persisted assignment state (not from a one-time push payload).\nResponse: FullName, WorkerCode, ImageUrl, Status, TodayJobCount, BreakCountUsed, CompletedJobCount, Nationality, WorkStartDate, Phone, and Shift name/start/end. Status is one of open_app, ready, assigned, waiting_team, working, break. When Status is break, response includes BreakUntil, BreakUntilUnixMs, and RemainingBreakTime. Status is assigned both while the assignment is still PENDING (worker has not pressed accept yet) and once it is ACCEPTED (waiting to scan) — the two sub-cases are told apart by which deadline pair is non-null: while PENDING, CurrentJob.AcceptDeadlineAt/AcceptDeadlineUnixMs are set and ScanDeadlineAt/ScanDeadlineUnixMs are null; once ACCEPTED, ScanDeadlineAt/ScanDeadlineUnixMs are set and AcceptDeadlineAt/AcceptDeadlineUnixMs are null. When Status is waiting_team or working (worker has already scanned), all four deadline fields are null. When Status is assigned, waiting_team, or working, response includes CurrentJob with TicketNumber, LicensePlate, LicensePlateProvince, VehicleType, TeamScan, Markets[] (each with TicketNo — scan its barcode via check-in-barcode, Booths[].Products[] using Gate Quantity), and Team[].ShirtNumber/FullName/ScanStatus/ScannedAt."
          },
          "response": [],
          "description": "Method: GET\nURL: {{baseUrl}}/api/workers/me/status\nAuth: Worker access token\nBody: None\nResponse: FullName, WorkerCode, ImageUrl, Status, TodayJobCount, BreakCountUsed, CompletedJobCount, Nationality, WorkStartDate, Phone, and Shift name/start/end. Status is one of open_app, ready, assigned, waiting_team, working, break. When Status is break, response includes BreakUntil, BreakUntilUnixMs, and RemainingBreakTime. When Status is assigned, response includes CurrentJob.ScanDeadlineAt and CurrentJob.ScanDeadlineUnixMs. When Status is waiting_team or working, CurrentJob.ScanDeadlineAt and CurrentJob.ScanDeadlineUnixMs are null. When Status is assigned, waiting_team, or working, response includes CurrentJob with TicketNo, LicensePlate, LicensePlateProvince, VehicleType, TeamScan, Markets[].Booths[].Products[] using Gate Quantity, and Team[].ShirtNumber/FullName/ScanStatus/ScannedAt."
        },
        {
          "name": "GET /api/workers/me/assignments/history",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/workers/me/assignments/history?date=2026-07-07",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "workers",
                "me",
                "assignments",
                "history"
              ],
              "query": [
                {
                  "key": "date",
                  "value": "2026-07-07",
                  "description": "Assignment created date in YYYY-MM-DD format."
                }
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL single date: {{baseUrl}}/api/workers/me/assignments/history?date=2026-07-07\nURL date range: {{baseUrl}}/api/workers/me/assignments/history?date_from=2026-08-01&date_to=2026-08-14\nAuth: Worker access token\nQuery: send either date, or date_from + date_to together. All fields use YYYY-MM-DD. date cannot be combined with date_from/date_to. Range is inclusive and limited to 31 calendar days. Body: None\nResponse: Date, Summary.JobCount, Summary.AcceptTimeoutJobCount, Summary.CompletedJobCount, and Data[]. Each assignment includes TicketNo, LicensePlate, Status, Markets[].Booths[].CompletedAt, Markets[].Booths[].Products[].ConfirmedQuantity, and Rating as a single integer 1-5 or null. CompletedAt is the per-booth GateTicket completion timestamp and is null when that booth/case is not complete. No earnings, TotalEarnings, booth amount, product FinalAmount, or worker payment breakdown are returned. Dates are interpreted in Asia/Bangkok time and filter by assignment CreatedAt using [startAt, dayAfterEndAt)."
          },
          "response": [],
          "description": "Method: GET\nURL single date: {{baseUrl}}/api/workers/me/assignments/history?date=2026-07-07\nURL date range: {{baseUrl}}/api/workers/me/assignments/history?date_from=2026-08-01&date_to=2026-08-14\nAuth: Worker access token\nQuery: send either date, or date_from + date_to together. All fields use YYYY-MM-DD. date cannot be combined with date_from/date_to. Range is inclusive and limited to 31 calendar days. Body: None\nResponse: Date, Summary.JobCount, Summary.AcceptTimeoutJobCount, Summary.CompletedJobCount, and Data[]. Each assignment includes TicketNo, LicensePlate, Status, Markets[].Booths[].CompletedAt, Markets[].Booths[].Products[].ConfirmedQuantity, and Rating as a single integer 1-5 or null. CompletedAt is the per-booth GateTicket completion timestamp and is null when that booth/case is not complete. No earnings, TotalEarnings, booth amount, product FinalAmount, or worker payment breakdown are returned. Dates are interpreted in Asia/Bangkok time and filter by assignment CreatedAt using [startAt, dayAfterEndAt)."
        },
        {
          "name": "GET /api/workers/me/earnings/summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/workers/me/earnings/summary",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "workers",
                "me",
                "earnings",
                "summary"
              ]
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/workers/me/earnings/summary\nAuth: Worker access token\nQuery: None\nBody: None\nResponse: Period.FromDate/ToDate/DayCount, TotalEarnings, Daily[15], and Details. Backend returns latest 15 completed Bangkok calendar days and excludes the current day. Details.Earnings is read from persisted TicketWorker.FinalEarningAmount."
          },
          "response": [],
          "description": "Method: GET\nURL: {{baseUrl}}/api/workers/me/earnings/summary\nAuth: Worker access token\nQuery: None\nBody: None\nResponse: Period.FromDate/ToDate/DayCount, TotalEarnings, Daily[15], and Details. Backend returns latest 15 completed Bangkok calendar days and excludes the current day. Details.Earnings is read from persisted TicketWorker.FinalEarningAmount."
        },
        {
          "name": "POST /api/workers/me/online",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/workers/me/online",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "workers",
                "me",
                "online"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/workers/me/online\nAuth: Worker access token\nBody: None\nResponse: StatusCode, Code, Message. Success response is { \"StatusCode\": 200, \"Code\": \"WORKER_ONLINE_SUCCESS\", \"Message\": \"Worker entered queue successfully.\" }.\nNotes: Requires active /ws/workers WebSocket. From open_app, worker can enter queue only once per shift/day. If current status is break, worker can call this endpoint to return early before break timer ends. Returns 403 if the worker has no current schedule or is outside the assigned shift. OUTSIDE_WORK_SHIFT includes shift and remaining_time text for the mobile modal. Daily counts are returned by GET /api/workers/me/status."
          },
          "response": []
        },
        {
          "name": "POST /api/workers/me/offline",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/workers/me/offline",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "workers",
                "me",
                "offline"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/workers/me/offline\nAuth: Worker access token\nBody: None\nResponse: StatusCode, Code, Message. Success response is { \"StatusCode\": 200, \"Code\": \"WORKER_OFFLINE_SUCCESS\", \"Message\": \"Worker left queue successfully.\" }. The worker is removed from Redis queue internally and the current shift is closed so the worker cannot re-enter queue in the same shift. Daily counts are returned by GET /api/workers/me/status."
          },
          "response": []
        },
        {
          "name": "POST /api/workers/me/break",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/workers/me/break",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "workers",
                "me",
                "break"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/workers/me/break\nAuth: Worker access token\nBody: None\nResponse: FullName, WorkerCode, status, BreakCountUsed, BreakCountLimit. status is break.\nNotes: Puts worker into break status for the configured duration, default 15 minutes. Requires worker to be ready and inside the current shift time. OUTSIDE_WORK_SHIFT includes shift and remaining_time text for the mobile modal. Default max is 4 breaks per shift from WorkerBreakLimit. BullMQ returns worker to the tail of Redis queue automatically when break ends; worker can call /online early while status is break."
          },
          "response": []
        },
        {
          "name": "POST /api/workers/me/assignments/:ticketNumber/accept",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const body = pm.response.json();",
                  "const firstMarket = body.Markets?.[0];",
                  "const firstStall = firstMarket?.Stalls?.[0];",
                  "if (firstStall?.BoothCode) { pm.collectionVariables.set('boothCode', String(firstStall.BoothCode)); pm.collectionVariables.set('stallCode', String(firstStall.BoothCode)); }",
                  "if (firstMarket?.TicketNo) pm.collectionVariables.set('ticketNo', String(firstMarket.TicketNo));",
                  "pm.test('accepted worker detail response', () => {",
                  "  pm.expect(body).to.have.keys(['LicensePlate', 'LicensePlateProvince', 'ScanDeadlineAt', 'ScanDeadlineUnixMs', 'Team', 'Markets']);",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/workers/me/assignments/{{ticketNumber}}/accept",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "workers",
                "me",
                "assignments",
                "{{ticketNumber}}",
                "accept"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/workers/me/assignments/{{ticketNumber}}/accept\nAuth: Worker access token\nBody: None\nPath: {{ticketNumber}} comes from WORKER_ASSIGNED.ticketNumber.\nNotes: Assignment must be PENDING and accepted within WorkerAcceptDeadlineSeconds, default 60 seconds. If not accepted in time, assignment becomes TIMEOUT and the worker returns to the tail of the queue even when the Worker WebSocket is disconnected. The consecutive accept-timeout streak resets after a successful accept. After WorkerAcceptTimeoutLimit consecutive accept timeouts, default 3, the worker is moved to open_app and cannot re-enter the queue in the same shift. Response is trimmed for the worker job detail screen only: LicensePlate, LicensePlateProvince, ScanDeadlineAt, ScanDeadlineUnixMs, team[].FullName/WorkerCode/ImageUrl/ScanStatus, markets[].TicketNo/marketName/StallCount/stalls[].boothCode/boothName/ProductCount/products[].productCode/productName/quantity/packageName. ScanDeadlineAt is the persisted assignment QR scan deadline in ISO 8601 UTC; ScanDeadlineUnixMs is the same value in Unix milliseconds for mobile countdown. markets[].TicketNo is the Business Ticket id printed as a barcode on the physical Gate paper ticket — send it as TicketNo to POST .../check-in-barcode next (no {{ticketNumber}} needed on either call — both resolve the vehicle from this worker's own current active assignment). Use the same TicketNo and stalls[].BoothCode later with POST /api/workers/me/assignments/tickets/complete."
          },
          "response": []
        },
        {
          "name": "POST /api/workers/me/assignments/check-in-barcode",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/workers/me/assignments/check-in-barcode",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "workers",
                "me",
                "assignments",
                "check-in-barcode"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/workers/me/assignments/check-in-barcode\nAuth: Worker access token\nPath: none — the vehicle (TicketNumber) is always resolved from this worker's own current active assignment, never from the URL. A worker never has more than one active assignment at a time.\nBody: TicketNo (required) — one Business Ticket's ticket_no, the value printed as a barcode on the physical Gate paper ticket. Also readable from Markets[].TicketNo in the accept response or CurrentJob.Markets[].TicketNo from GET /api/workers/me/status. It is a real business identifier (not a generated secret), resolved safely by scoping the lookup to this worker's own current vehicle assignment. Scanning any one Business Ticket under the vehicle checks in the whole team.\nResponse: Status, WorkerStatus, WorkerCode, TicketNumber, TicketNo, TeamScan. TicketNo echoes back the exact Business Ticket this request scanned (never any other TicketNo under the same TicketNumber, even when the vehicle has several). Normally { \"Status\": \"SCANNED\", \"WorkerStatus\": \"waiting_team\", \"WorkerCode\": \"MN000142\", \"TicketNumber\": \"20260804000001\", \"TicketNo\": \"20260804000002\", \"TeamScan\": { \"WorkersRequired\": 3, \"CheckedInCount\": 1, \"RemainingCount\": 2, \"IsReady\": false } }.\nNotes: Checks worker into the job only while ScanDeadlineAt is still valid. Default scan window comes from WorkerScanDeadlineMinutes after accept. After this worker scans, WorkerStatus is waiting_team until every active/eligible assignment in the team has scanned, then working. Admin SSE receives ASSIGNMENT_SCAN_DEADLINE_WARNING when an ACCEPTED worker has not checked in and remaining scan time is below WorkerScanWarningBeforeMinutes. In a multi-worker team, once the first worker checks in, remaining ACCEPTED workers receive ASSIGNMENT_SCAN_DEADLINE_SHORTENED and their ScanDeadlineAt/ScanDeadlineUnixMs is shortened by WorkerScanTeamRemainingMinutes from that first check-in. If scan time expires by API call or background timeout, the assignment becomes TIMEOUT and worker status returns to open_app. Admin can extend ScanDeadlineAt/ScanDeadlineUnixMs and the worker can retry with the same TicketNo if the assignment is still ACCEPTED. If active/eligible team scan is ready, vehicle job moves to WORKING. The scan is also recorded on the SCANNED WorkerAssignmentEvent with which TicketNo/MarketCode/MarketName was scanned, auditable later.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"TicketNo\": \"{{ticketNo}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": []
        },
        {
          "name": "GET /api/workers/me/products/:productCode/packages",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{workerAccessToken}}",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/workers/me/products/{{productCode}}/packages",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "workers",
                "me",
                "products",
                "{{productCode}}",
                "packages"
              ]
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/workers/me/products/{{productCode}}/packages\nAuth: Worker access token\nUse when a worker counts produce at a booth and finds it is actually a different package size than what Gate originally declared for that product line (not just a different quantity). productCode must be one already present on the ticket being edited (from the accept/check-in/status response's product lines) — this only looks up alternate packages of that same product, it does not switch to a different product.\nResponse: productCode, productName, and packages[] with packageCode (send back as the new PackageCode on submit), packageName (UI display only), packageWeight.\nNotes: 404 if no active product matches productCode.",
            "response": []
          },
          "response": []
        },
        {
          "name": "POST /api/workers/me/assignments/tickets/complete",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Production-like response does not expose LINE postback tokens.",
                  "// Vendor confirm/reject should come from the real LINE OA button postback."
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"TicketNo\": \"{{ticketNo}}\",\n  \"BoothCode\": \"{{boothCode}}\",\n  \"Items\": [\n    {\n      \"ProductCode\": \"{{productCode}}\",\n      \"PackageCode\": \"{{packageCode}}\",\n      \"ConfirmedQuantity\": 8\n    }\n    // To switch this product's package instead of just its quantity: call GET\n    // /api/workers/me/products/{ProductCode}/packages first, then send the new\n    // PackageCode plus OriginalPackageCode (the PackageCode Gate originally declared\n    // for this product line), e.g. { \"ProductCode\": \"{{productCode}}\", \"PackageCode\": \"<new>\", \"OriginalPackageCode\": \"{{packageCode}}\", \"ConfirmedQuantity\": 8 }\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/workers/me/assignments/tickets/complete",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "workers",
                "me",
                "assignments",
                "tickets",
                "complete"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/workers/me/assignments/tickets/complete\nAuth: Worker access token\nBody: TicketNo, BoothCode and Items required. TicketNo names which Business Ticket this submission belongs to — required because two Business Tickets under the same vehicle, against different markets, can legitimately share the same BoothCode; TicketNo plus BoothCode together resolve the exact booth unambiguously. Send BoothCode in body so codes containing / such as OM3/40 do not need URL encoding. Send every ProductCode + PackageCode in the ticket with ConfirmedQuantity counted by the worker.\nPackage switch: to change a product's package (not just quantity), first call GET /api/workers/me/products/{ProductCode}/packages to find a valid new PackageCode, then send that item with PackageCode set to the new value and OriginalPackageCode set to the PackageCode Gate originally declared for that product line. Backend re-derives the rate snapshot (weight/rate can differ by package) before accepting — an invalid/inactive PackageCode is rejected with 409 PRODUCT_PACKAGE_NOT_FOUND. Items not switching a package only need PackageCode as before.\nFinancial boundary: This request only records confirmed actual quantities and starts vendor confirmation. It must not create final financial records before vendor confirm or timeout auto-confirm.\nNotes: No TicketNumber in the URL — the vehicle is always resolved from this worker's own current active assignment, never from the client. TicketNo and BoothCode come from the accepted/check-in job payload (Markets[].TicketNo / Markets[].Stalls[].BoothCode). Backend resolves each product by ProductCode + PackageCode. The LINE message to the vendor shows both the originally declared package and the new one when a product's package was switched. The response does not expose LINE postback tokens. Vendor confirm completes financialization; vendor reject returns the ticket to REJECT for resubmission."
          },
          "response": []
        }
      ]
    },
    {
      "name": "LINE",
      "item": [
        {
          "name": "POST /api/line/webhook - vendor confirm",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"events\": [\n    {\n      \"type\": \"postback\",\n      \"source\": {\n        \"userId\": \"{{lineUserId}}\"\n      },\n      \"postback\": {\n        \"data\": \"token={{vendorConfirmToken}}\"\n      }\n    }\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/line/webhook",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "line",
                "webhook"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/line/webhook\nAuth: No auth. LINE signature is required only when LINE_CHANNEL_SECRET is configured.\nBody: Manual mock LINE postback using the same token-only format as the real LINE Flex buttons. source.userId should use {{lineUserId}} from the seed mapping. In normal use, vendor taps Confirm in LINE OA and this request is sent by LINE, not Postman. Confirms the waiting ticket completion, notifies worker/admin, and sends a 1-5 optional rating Flex prompt to the confirming LINE user. If this confirmation makes every ticket in the vehicle job terminal, backend closes the market/vehicle job and sets AssignmentStatus to COMPLETED so workers can go online for the next job. If there is another stall job, AssignmentStatus returns to WORKING. If vendor taps a rating score, backend replies with a thank-you Flex card and a temporary 0.00 baht service-charge summary card."
          },
          "response": []
        },
        {
          "name": "POST /api/line/webhook - vendor reject",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"events\": [\n    {\n      \"type\": \"postback\",\n      \"source\": {\n        \"userId\": \"{{lineUserId}}\"\n      },\n      \"postback\": {\n        \"data\": \"token={{vendorRejectToken}}&reject_reason=Quantity mismatch\"\n      }\n    }\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/line/webhook",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "line",
                "webhook"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/line/webhook\nAuth: No auth. LINE signature is required only when LINE_CHANNEL_SECRET is configured.\nBody: Manual mock LINE postback using the same token-only format as the real LINE Flex buttons. source.userId should use {{lineUserId}} from the seed mapping. In normal use, vendor taps Reject in LINE OA and this request is sent by LINE, not Postman. Optional RejectReason is saved to the stall job and returned by Admin operations/history until the worker resubmits. Rejects the waiting ticket completion, sends a reject result Flex card to vendor, sets AssignmentStatus to REJECT, then the worker can submit the ticket completion again."
          },
          "response": []
        },
        {
          "name": "POST /api/line/webhook - vendor rating",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"events\": [\n    {\n      \"type\": \"postback\",\n      \"source\": {\n        \"userId\": \"{{lineUserId}}\"\n      },\n      \"postback\": {\n        \"data\": \"token={{vendorRatingToken}}&score=5\"\n      }\n    }\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/line/webhook",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "line",
                "webhook"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/line/webhook\nAuth: No auth. LINE signature is required only when LINE_CHANNEL_SECRET is configured.\nBody: Manual mock LINE rating postback. source.userId should use {{lineUserId}} from the seed mapping. Score must be 1-5. In normal use, vendor taps one of the optional rating buttons after confirming completion. Backend stores one rating per Ticket and LINE user; if the same LINE user taps again, the latest score replaces the old one. After a valid rating, backend sends a thank-you Flex card and a temporary 0.00 baht service-charge summary card."
          },
          "response": []
        },
        {
          "name": "GET /api/line/dev",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/line/dev",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "line",
                "dev"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/line/dev\nAuth: No auth.\nResponse: text/html standalone page listing submissions with Confirm/Reject buttons. This is a fallback tool to confirm or reject vendor ticket completions without going through LINE, used when the LINE messaging line is rate-limited. It never sends messages through the LINE Messaging API and is enabled in every environment including production, so open it directly in a browser rather than through Postman."
          },
          "response": []
        },
        {
          "name": "GET /api/line/dev/submissions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/api/line/dev/submissions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "line",
                "dev",
                "submissions"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: GET\nURL: {{baseUrl}}/api/line/dev/submissions\nAuth: No auth.\nResponse: Data array of ticket completion submissions (SubmissionId, TicketId, statuses, booth/market info, submitted products, and Actionable flag). Actionable is true only for the current waiting submission that can still be confirmed or rejected. Copy a SubmissionId from here into {{lineDevSubmissionId}} to use the confirm/reject requests below. Part of the LINE dev tester fallback for when the LINE line is rate-limited; enabled in every environment including production."
          },
          "response": []
        },
        {
          "name": "POST /api/line/dev/submissions/:submissionId/confirm",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/line/dev/submissions/{{lineDevSubmissionId}}/confirm",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "line",
                "dev",
                "submissions",
                "{{lineDevSubmissionId}}",
                "confirm"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/line/dev/submissions/{{lineDevSubmissionId}}/confirm\nAuth: No auth.\nBody: None required.\nConfirms the waiting ticket completion submission using the same completion flow as the real LINE vendor confirm button, but does not send any LINE message. Fails with 404 SUBMISSION_NOT_FOUND if the id does not exist, or 409 SUBMISSION_ALREADY_HANDLED if it was already confirmed, rejected, or superseded by a newer submission. Fallback tool for when the LINE line is rate-limited; enabled in every environment including production, so treat SubmissionId carefully — this mutates real ticket/vehicle job state."
          },
          "response": []
        },
        {
          "name": "POST /api/line/dev/submissions/:submissionId/reject",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              },
              {
                "key": "X-Request-Id",
                "value": "{{$guid}}",
                "type": "text",
                "description": "Optional. Correlation id for this request — Postman's {{$guid}} generates a fresh UUID every time this request is sent. Echoed back in the X-Request-Id response header and in every error response body."
              },
              {
                "key": "X-Client-Version",
                "value": "{{clientVersion}}",
                "type": "text",
                "description": "Optional. Version of the calling client app, recorded in server logs for debugging only."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"reject_reason\": \"Quantity mismatch\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/api/line/dev/submissions/{{lineDevSubmissionId}}/reject",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "api",
                "line",
                "dev",
                "submissions",
                "{{lineDevSubmissionId}}",
                "reject"
              ]
            },
            "auth": {
              "type": "noauth"
            },
            "description": "Method: POST\nURL: {{baseUrl}}/api/line/dev/submissions/{{lineDevSubmissionId}}/reject\nAuth: No auth.\nBody: Optional RejectReason (max 1000 chars).\nRejects the waiting ticket completion submission using the same completion flow as the real LINE vendor reject button, but does not send any LINE message. Fails with 404 SUBMISSION_NOT_FOUND if the id does not exist, or 409 SUBMISSION_ALREADY_HANDLED if it was already confirmed, rejected, or superseded by a newer submission. Fallback tool for when the LINE line is rate-limited; enabled in every environment including production, so treat SubmissionId carefully — this mutates real ticket/vehicle job state."
          },
          "response": []
        }
      ]
    }
  ]
}
